# ביקורת גרסה 2 — ממצאים מאומתים

מועצת ביקורת: 9 ממדים, 140 סוכנים, 1237 קריאות כלים.
151 ממצאים גולמיים ← 92 אושרו באימות אדוורסרי, 39 נזרקו.

## ממצאים מאומתים

### 1. [high] Word-level disambiguation ("למה התכוונת?") is unreachable — it lives in handleUpdate, which only the INVENTORY textarea submits
**תיקון:** Minimal fix — lift the word-match into one helper and call it from the two real shopping entry points.

1. Add a component-level helper (place it after `stripName`/`suggestionPool` so it can reuse them):

    const findWordChoices = (name: string): string[] => {
      const norm = (n: string) => stripName(n).toLowerCase();   // strips leading emoji + "(N)"
      const stripPrefix = (w: string) => w.replace(/^(ה|ב|ל|מ|ש|ו|כ)/, '');
      const words = norm(name).split(/\s+/).filter(w => w.length > 1);
      if (words.length < 2) return [];
      const known = new Set<string>();
      safeInventory.forEach(i => known.add(norm(i.item_name || '')));
      safeShoppingList.forEach(s => known.add(norm(s.item_name || '')));
      const out: string[] = [];
      for (const w of words) for (const c of [w, stripPrefix(w)]) {
        if (c.length > 1 && known.has(c) && !out.includes(c)) out.push(c);
      }
      return out;
    };

2. In `handleQuickAddSubmit`, inside the row loop right before the insert at :652, add:

    const choices = findWordChoices(name);
    if (choices.length > 0) {
      wordChoices.push({ originalName: name, options: choices, item: { name, item_name: name, category

### 2. [medium] "חלוקה" panel ignores category_auto: the flag is written by one insert path out of three and read by none, so AI-guessed categories never reach a review queue
**תיקון:** Four small edits in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx; no type or migration change needed (select('*') + any[] already carry the field, DB default is false).

1. :652 — persist the flag on the main add path:
   await supabase.from('shopping_list').insert([{ item_name: name, category: cat, category_auto: !userPicked(row) }]);
   (Leave :834 alone — adding inside a category IS a user pick, and the column defaults to false.)

2. :1465 — include auto-classified items, keep כללי items:
   const klaliItems = filteredShoppingList.filter(s => s.category_auto || !s.category || s.category === 'כללי');

3. :1486 — preselect and label the current guess instead of hard-coding 'כללי':
   const current = item.category || 'כללי';
   const chosen = categorizeAssignments[item.id] ?? current;
   and render the guess next to the name, e.g. {item.category_auto && <span className="text-xs text-indigo-600 font-bold">🤖 {current}</span>}.

4. :782 and :801 — let a כללי (or unchanged) selection count as a confirmation that clears the flag:
   const entries = Object.entries(categorizeAssignments).filter(([, v]) => !!v);
   ...
   await supabase.from('shopping_list').u

### 3. [high] Req 18 — the completion celebration on the working branch is the rejected dot/ball burst, not the upgraded fireworks
**תיקון:** Do not write the effect from scratch — port the already-approved version from the unmerged branch. Minimal, mechanical change:

1. globals.css: replace the `firework-particle` / `firework-flash` blocks (lines 145-157) and the `.animate-firework-particle` / `.animate-firework-flash` classes (lines 174-181) with the 5 keyframes and 4 classes from the branch reference (`firework-launch`, the richer `firework-flash`, `firework-streak`, `firework-spark`, `firework-twinkle`, plus `.animate-firework-launch`, `.animate-firework-streak`, `.animate-firework-spark` — note `.animate-firework-spark` composes two animations: `firework-spark 1500ms ... forwards, firework-twinkle 600ms ease-in-out infinite`). Source: `git show origin/feature/tasks-edit-dependencies:src/app/globals.css`.

2. page.tsx: replace `buildCelebration` (955-987) with the branch version at lines 444-496 — 12 bursts, each with `streaks` (38-52 rays, fly 150-240px, width 70-110px, height 3-4px) and `sparks` (18-25, dist 30-160px, own stagger delay) and a `secondaryColor`.

3. page.tsx: replace the celebration overlay JSX (1167-1198, the flash span + particles map) with the branch version at lines 640-730 — launch trail span, 

### 4. [high] Req 1 — duplicate detection ignores leading emoji, so an emoji-prefixed stored name and the typed bare name are added twice
**תיקון:** Make one shared key function that strips leading decoration too, and use it in every duplicate check. Minimal edit — add at module scope in src/app/page.tsx (above the component):

const stripDecor = (n: string) => (n || '').replace(/^[^A-Za-z0-9֐-׿]+/, '');
const itemKey = (n: string) => stripDecor(n).replace(/\s*\(\d+\)\s*$/, '').trim().toLowerCase();

Then replace the local `normalize` / `normalizeShopName` definitions at :358, :551, :590, :619, :683, :816 with `itemKey` (delete the local consts and use itemKey at :400, :412, :604, :644, :699, :830), and replace the two inline expressions at :486-487 with itemKey(...). Also point the existing stripName at the same prefix regex so the pool and the keys stay in sync: `const stripName = (n: string) => stripDecor(n).replace(/\s*\(\d+\)\s*$/, '').trim();`

Both sides of the guessCategories map (:604 write, :641/:697 read) use the same function, so changing it uniformly keeps those lookups consistent. Display/insert still store the original name — only the comparison key changes.

### 5. [medium] Req 9 — category rename/delete only rewrites shopping_list, so inventory rows keep the old name and the category is re-derived into displayCategories
**תיקון:** Add the matching `inventory_items` statement in each handler (2 lines total).

In saveRenameCategory, after line 571:
    await supabase.from('inventory_items').update({ category: newCat }).eq('category', oldCat);

In deleteCategory, after line 580:
    await supabase.from('inventory_items').update({ category: 'כללי' }).eq('category', cat);

That makes both tables that feed displayCategories agree, so the renamed category shows as one header and the deleted one actually disappears (matching the confirm text). Optional hardening, outside the minimal fix: the rename at 572 can hit a duplicate on `category_order.category_name` if the target name already exists — worth an upsert/delete-then-insert if that column is unique.

### 6. [low] Req 17 — clicking a chip on an already-open card does not focus that field
**תיקון:** Add an imperative focus pass keyed on the focus state, and drop the mount-time `autoFocus` on the task-edit fields so there is one mechanism instead of two.

1. Near the other refs (~:140) add a ref map:

    const editFieldRefs = useRef<Record<string, HTMLElement | null>>({});

2. Add an effect after it:

    useEffect(() => {
      if (!editingTaskId) return;
      const key = editTaskFocus === 'all' ? 'title' : editTaskFocus;
      editFieldRefs.current[key]?.focus();
    }, [editingTaskId, editTaskFocus]);

3. On each edit control (:1819 title, :1833 urgency, :1841 assignee, :1851 date, :1859 depends) replace the `autoFocus={...}` prop with a ref callback, e.g. for the date input:

    ref={el => { editFieldRefs.current.date = el; }}

and likewise `title`, `urgency`, `assignee`, `depends`.

Because the effect depends on both `editingTaskId` and `editTaskFocus`, it covers the first open (previously handled by `autoFocus`) and the chip click on an already-open card (previously handled by nothing). The single ref map is safe with only one card in edit mode at a time, which `editingTaskId` already guarantees; entries for unmounted cards are overwritten with `null` by the ref callba

### 7. [low] Req 7 — the per-category "+ הוסף" field has no autocomplete (inventory textarea claim is wrong)
**תיקון:** Attach the existing suggestion logic to the per-category input only; leave the inventory textarea alone.

1. Add one state next to `categoryAddInput` (page.tsx:108): `const [categoryAddSuggest, setCategoryAddSuggest] = useState(false);`

2. At page.tsx:1704, wrap the input in `<div className="relative flex-1 min-w-[160px]">` (moving `flex-1 min-w-[160px]` off the input), add to the input `onFocus={() => setCategoryAddSuggest(true)}`, `onBlur={() => window.setTimeout(() => setCategoryAddSuggest(false), 150)}`, `autoComplete="off"`, and render below it the same panel as :1394-1404, computed on the last comma-separated segment so multi-item entry still works:

    const seg = categoryAddInput.split(/[,،]/).pop() || '';
    const cq = stripName(seg).toLowerCase();
    const catMatches = (categoryAddSuggest && cq.length >= 1)
      ? suggestionPool.filter(s => { const sl = s.toLowerCase(); return sl.includes(cq) && sl !== cq; }).slice(0, 7)
      : [];

   Each option's `onMouseDown` should `e.preventDefault()` and replace only that trailing segment:

    const parts = categoryAddInput.split(/[,،]/);
    parts[parts.length - 1] = ` ${m}`;
    setCategoryAddInput(parts.join(',').replace(

### 8. [low] Req 14 — pressing Enter in the new-task form saves AND opens WhatsApp, bypassing the two-button choice
**תיקון:** Point the implicit submit at the plain save, so Enter behaves like the 💾 שמור button. In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:910-913 change:

  const handleAddTask = async (e: React.FormEvent) => {
    e.preventDefault();
    await persistNewTask(false);
  };

That is the whole fix — one word. Both buttons keep their explicit onClick handlers, so the two-way choice is preserved and Enter no longer navigates away. (Equivalent alternative: onSubmit={e => { e.preventDefault(); persistNewTask(false); }} at :1746 and delete handleAddTask, which is now used nowhere else — grep shows its only reference is that form.) globals.css needs no change; it is not involved in this defect.

### 9. [low] Req 23 — camping cart item still seeded as '🛒 עגלת קמפינג'; 🚼 exists nowhere in the repo or its git history
**תיקון:** Two parts — the seed text and the already-seeded rows.

1) Change the literal in both seed files (identical line in each):
   supabase-migration-camping-template.sql:32 and supabase-הרצה-אחת-מסודרת.sql:114
   ('🛒 עגלת קמפינג', ...)  →  ('🚼 עגלת קמפינג', ...)

2) Add an idempotent fixup so the DB the user already seeded is corrected. It must touch BOTH tables, not just one: equipment_items (what the רשימות screen reads, filled by supabase-סידור-רשימות.sql) and shopping_template_items (still holds the row in any household that has not run סידור-רשימות, which deletes the template):

UPDATE equipment_items        SET item_name = '🚼 עגלת קמפינג' WHERE item_name = '🛒 עגלת קמפינג';
UPDATE shopping_template_items SET item_name = '🚼 עגלת קמפינג' WHERE item_name = '🛒 עגלת קמפינג';

Prefer these exact-match UPDATEs over the proposed `replace(item_name,'🛒','🚼') WHERE item_name LIKE '%עגלת%'` — same effect today (only one עגלת item exists) but it won't quietly rewrite any future עגלת item that legitimately carries 🛒.

Ordering note: run the UPDATE on equipment_items whenever the seed literal changes. supabase-סידור-רשימות.sql:39-44 dedupes with `NOT EXISTS ... AND e.item_name = i.item_name`, so

### 10. [high] fetchData appends to pendingItems from inside a filter predicate — classification cards multiply on every refresh (and never clear)
**תיקון:** Make the derivation pure and idempotent — replace the DB-derived portion of the state instead of appending, while preserving the parse-flow cards set at line 450 (those have no DB row yet, and a late-resolving `fetchData()` from `saveItemWithCategory` would otherwise wipe them). Replace lines 160-169 with:

    if (invRes.data) {
      const isUncertain = (i: any) => i.category === 'uncertain' || i.category === 'לא ידוע';
      const dbPending = invRes.data
        .filter(isUncertain)
        .map((i: any) => ({ ...i, needs_classification: true }));
      setInventory(invRes.data.filter((i: any) => !isUncertain(i)));
      setPendingItems(prev => {
        const locals = prev.filter(p => !p.id); // parse-flow cards, not yet in the DB
        const names = new Set(locals.map(p => p.item_name));
        return [...locals, ...dbPending.filter(p => !names.has(p.item_name))];
      });
    }

Also switch the card keys at 1252 from `pending-${idx}` to the item id where present, so React does not reuse card state across a refetch.

Companion one-liner needed for the card to actually go away (same root cause, otherwise it just stops multiplying while still reappearing once per resolve): i

### 11. [high] Resolving an uncertain inventory item inserts a duplicate row and leaves the original stuck on category 'uncertain'
**תיקון:** In handleResolvePending, update the existing row by id when the pending item came from the DB, and only fall back to saveItemWithCategory for parser-derived items with no id. Keep the new-category registration that saveItemWithCategory did:

```ts
const handleResolvePending = async (item: Item, selectedCategory: string) => {
  setPendingItems(prev => prev.filter(p => p.item_name !== item.item_name));

  const cat = (selectedCategory || 'כללי').trim() || 'כללי';
  if (cat !== 'כללי' && !categories.includes(cat)) {
    await supabase.from('category_order').insert([{ category_name: cat, sort_order: 99 }]);
    setCategories(prev => Array.from(new Set([...prev, cat])));
  }

  if (item.id) {
    // Row already exists in inventory_items (it was filtered out of `inventory`
    // by fetchData because its category was 'uncertain') — fix it in place.
    await supabase.from('inventory_items').update({ category: cat }).eq('id', item.id);
    await fetchData();
  } else {
    await saveItemWithCategory({ ...item, category: cat });
  }

  if (pendingItems.length <= 1 && disambiguationItems.length === 0) showStatus('✅ מעודכן!', true);
};
```

Two cheap companions worth doing in the same change

### 12. [medium] Classification card resolves against the current view, not the item's origin — a pending inventory item resolved in the SHOPPING view is written to shopping_list (renamed "name (qty)") and the inventory row stays uncertain
**תיקון:** Carry the target on the pending item instead of deriving it from `activeView`:

1. Tag at creation.
   Line 163: `setPendingItems(prev => [...prev, { ...i, needs_classification: true, target: 'INVENTORY' }]);`
   Line 450: `...({ ...i, item_name: i.name || i.item_name || 'פריט', target: activeView }))`

2. Branch on the tag in `saveItemWithCategory` (line 315). Add at the top of the function:
   `const target = item.target || activeView;`
   then change line 315-ish `if (activeView === 'INVENTORY')` to `if (target === 'INVENTORY')` and line 325 to `} else if (target === 'SHOPPING') {`. Direct callers (certainItems loop) pass no `target`, so they keep today's view-derived behaviour.

3. For a source-1 item, update the existing row instead of inserting a duplicate: in the INVENTORY branch prefer `item.id` when present —
   `let existing = item.id ? { id: item.id, quantity: 0 } as any : inventory.find(i => i.item_name === name);`
   or simply `if (item.id) { await supabase.from('inventory_items').update({ category: cat }).eq('id', item.id); }` before the find/insert path.

With the target explicit, the line-1250 render gate can stay as-is (the card just resolves correctly from either 

### 13. [medium] handleUpdate's removal loop reads a stale `inventory` snapshot — two removals resolving to the same row silently lose one decrement
**תיקון:** Keep a local running-quantity map inside `handleUpdate` and feed the running value into `executeRemoval`, so each iteration builds on the previous write instead of the render snapshot.

In `handleUpdate`, before the `for (const item of data.items)` loop (around src/app/page.tsx:361):

    const workingQty = new Map(inventory.map(i => [i.id, i.quantity]));

Then replace the single-match write at src/app/page.tsx:387-389:

    } else if (matches.length === 1) {
      const row = matches[0];
      const cur = workingQty.get(row.id) ?? row.quantity;
      const next = item.removeAll ? 0 : Math.max(0, cur + item.quantity);
      workingQty.set(row.id, next);
      await executeRemoval({ ...row, quantity: cur }, item.quantity, item.removeAll);
    }

Passing `{ ...row, quantity: cur }` leaves `executeRemoval`/`updateExactQuantity` arithmetic, the `finalQty === item.quantity` guard, and the `finalQty < item.quantity` low-stock comparison all operating on the running value rather than the stale one — no signature change needed.

Apply the same running-value treatment to the addition path if you want the duplicate-mention case fixed too: give `saveItemWithCategory` an explicit current-quant

### 14. [high] Supabase errors are never inspected — failed writes are reported as success
**תיקון:** Minimal, no call-site churn: add a throwing wrapper in src/lib/supabase.ts and use it for writes, so the existing try/catch blocks actually engage.

In src/lib/supabase.ts:
export const mustWrite = async <T>(q: PromiseLike<{ data: T; error: unknown }>) => {
  const { data, error } = await q;
  if (error) throw error;
  return data;
};

Then wrap the write calls, e.g. page.tsx:653
await mustWrite(supabase.from('shopping_list').insert([{ item_name: name, category: cat }]));
and identically at 708, 835, 862, plus the category_order inserts at 650/705/797 and the updates at 801.
Because these sit inside try blocks whose catch already calls showStatus('❌ שגיאה בשמירה', true), the throw now surfaces. Move `summary.push(...)` / `added++` after the awaited write (they already are) and leave the green panel driven by `summary`, which will then be empty on failure. In handleQuickAddSubmit, also drop the `if (added > 0) showStatus('')` suppression when the loop threw — the catch already overrides it.

For the two optimistic deletes, either refetch or roll back:
259: const { error } = await supabase.from('inventory_items').delete().eq('id', item.id);
     if (error) { showStatus('❌ שגיאה במחיק

### 15. [high] HTTP status of /api/parse is never checked — status pill stays on 'מעבד...' and the user's text is discarded
**תיקון:** In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, in handleUpdate right after line 338, add the status check and move the input clear so it only runs on a real parse:

      const res = await fetch('/api/parse', { method: 'POST', body: JSON.stringify({ text: input, categories: categories }) });
      if (!res.ok) { showStatus('❌ שגיאה בעיבוד', true); return; }
      const data = await res.json();
      if (!data.items || data.items.length === 0) { showStatus('❌ לא זוהו פריטים', true); return; }

Both early returns leave `input` intact so the user can retry or edit. Line 462's `setInput('')` then only executes after the 350-460 block actually ran; the `if (data.items && data.items.length > 0)` guard at 350 can stay as-is (now always true) or be dropped.

Apply the identical two lines in handleEquipmentUpdate after line 1064 (`if (!res.ok) {...} ` before the json parse, plus the empty-items guard before 1065), leaving `setInput('')` at 1092 reachable only on success.

Leave guessCategories unchanged — its silent 'כללי' fallback is intentional.

### 16. [medium] persistNewTask aborts silently when the insert fails — the save button appears dead
**תיקון:** In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, replace lines 898-900 with:

    if (!newTask.title) { showStatus('❌ חסר שם משימה', true); return; }
    const payload = { ...newTask, target_date: newTask.target_date || today };
    const { error } = await supabase.from('tasks').insert([payload]);
    if (error) { showStatus('❌ שגיאה בשמירת המשימה', true); return; }

and use `payload` (not `newTask`) for `taskCopy` on line 901 so the WhatsApp message carries the same fallback date. Optionally harden buildWhatsAppLink (line 887):

    const dateParts = (t.target_date || today).split('-');

Same treatment is worth applying to `saveEditTask` (lines 937-951), which does an optimistic `setTasks` then awaits the update without checking `error` at all — a failed update leaves the UI showing a value the DB never accepted until the next fetchData.

### 17. [low] handleUpdate replaces pendingItems, dropping the DB-derived uncertain cards
**תיקון:** Append instead of replace, de-duplicated by item_name (de-dup is needed anyway because fetchData at line 163 appends without checking):

    } else if (uncertainItems.length > 0) {
      const incoming = uncertainItems.map((i: any) => ({ ...i, item_name: i.name || i.item_name || 'פריט' }));
      setPendingItems(prev => {
        const seen = new Set(prev.map(p => p.item_name));
        return [...prev, ...incoming.filter(i => !seen.has(i.item_name) && !seen.has(i.item_name) )];
      });

i.e. filter incoming by `!seen.has(i.item_name)`, adding each accepted name to `seen` as you go so a doubled name inside one submit is also collapsed. Optionally apply the same guard at line 163 so repeated fetchData calls stop stacking duplicate cards.

### 18. [medium] Category rename and delete ignore inventory_items — categories resurrect and items are orphaned
**תיקון:** Add the matching `inventory_items` write to both functions (2 lines total), mirroring the existing `shopping_list` calls:

In `saveRenameCategory`, after the `shopping_list` update:

    await supabase.from('inventory_items').update({ category: newCat }).eq('category', oldCat);

In `deleteCategory`, after the `shopping_list` update:

    await supabase.from('inventory_items').update({ category: 'כללי' }).eq('category', cat);

Optionally add `shopping_template_items` the same way so revived templates do not reintroduce the old name. A single RPC/transaction is nicer but not required — the two statements are idempotent and `fetchData()` already refetches both tables afterwards, so a partial failure just leaves the pre-existing split state rather than a new corruption.

### 19. [low] addTemplateToShopping has no in-batch de-dup, so a repeated name in a template is inserted twice
**תיקון:** Mirror lines 643-648 in the template loop. In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, add the set next to `const duplicates` (line 692) and the two guards inside the loop:

    const duplicates: string[] = [];
    const queued = new Set<string>();
    ...
      for (const it of itemsList) {
        const base = normalize(it.name);
        if (!base || queued.has(base)) continue;
        if (shoppingList.some(s => normalize(s.item_name || '') === base)) {
          duplicates.push(it.name);
          continue;
        }
        queued.add(base);

Note the placement: `queued.add(base)` goes after the shoppingList check (same as line 648), so an already-existing item is still reported once in `duplicates` rather than being swallowed. Optionally also de-duplicate in createTemplate (734-737) so the template never stores the repeated line at all: `.filter(Boolean)` followed by a Set on the trimmed lines.

### 20. [medium] Low-stock "add to shopping" suppression uses substring matching (wrong direction) and disagrees with the low-stock alert check
**תיקון:** Add one module-level normalized predicate (module level is required — `stripName` at page.tsx:1121 and `normalizeShopName` at 358 are both inside the page component, while InventoryCard is a sibling function component) and call it from both sites:

const shopKey = (n: string) => (n || '')
  .replace(/^[^A-Za-z0-9֐-׿]+/, '')
  .replace(/\s*\(\d+\)\s*$/, '')
  .trim()
  .toLowerCase();

const isInShoppingList = (list: any[] | undefined, name: string) => {
  const k = shopKey(name);
  return !!k && (list || []).some(s => shopKey(s?.item_name || '') === k);
};

Then replace page.tsx:2307 with:
  const alreadyInShopping = isInShoppingList(shoppingList, item.item_name);
and page.tsx:240 with:
  const alreadyInShopping = isInShoppingList(shoppingList, item.item_name);

This makes both sites agree, keeps "חלב (3)" recognized as "חלב", stops "חלב סויה" from suppressing "חלב", and the `!!k` guard makes an empty name never match. No CSS change is needed — globals.css is not involved.

### 21. [medium] moveShoppingToInventory matches inventory by raw exact name, creating duplicate inventory rows
**תיקון:** In page.tsx replace lines 283-296 lookup/insert with a normalized name (stripName is in the same component scope and is only read at call time, exactly like rememberItemName at 265 already does):

  const moveShoppingToInventory = async (shopItem: any, qty: number) => {
    const clean = stripName(shopItem.item_name || '') || (shopItem.item_name || 'פריט');
    const existing = inventory.find(i => stripName(i.item_name || '').toLowerCase() === clean.toLowerCase());
    if (existing) {
      const newQty = (existing.quantity || 0) + qty;
      await supabase.from('inventory_items').update({ quantity: newQty }).eq('id', existing.id);
    } else {
      await supabase.from('inventory_items').insert([{
        item_name: clean, quantity: qty, category: shopItem.category || 'כללי',
        location: 'מזווה', household_id: '92e1a987-99b7-41ec-93fb-ae2ada2bcf72'
      }]);
    }

Keep the rest as-is (rememberItemName already strips internally). Optional same one-line hardening at 316: inventory.find(i => stripName(i.item_name || '').toLowerCase() === stripName(name).toLowerCase()).

### 22. [low] Dead SHOPPING branch in handleUpdate — the "למה התכוונת?" word-choice card can never render
**תיקון:** Pick one, don't leave both halves:

A) Retire the feature (smallest, matches the current structured quick-add UX): in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx delete the `if (activeView === 'SHOPPING') { … }` block at 398-436 including the `wordChoiceTasksLocal`/`duplicateShoppingItems`/`normalizeShopName`/`stripPrefix` locals at 355-359, the 441-442 and 447-449 branches and the 453-454/458-459 duplicate-status branches, collapse 369-370 to the literal `'inventory_items'` / `inventory`, then remove the now-orphaned `wordChoiceTasks` state (94), `handleResolveWordChoice` (484-495), the card at 1536-1569 and the `WordChoiceTask` type (28).

B) Keep the feature: lift the word-choice scan (the 407-425 block plus `stripPrefix`) into a small helper and call it from `handleQuickAddSubmit` per row before `await supabase.from('shopping_list').insert(...)` at 653 — push to a local list, `continue`, and `setWordChoiceTasks(prev => [...prev, ...local])` alongside the existing `setDuplicateShoppingAlerts` at 673. `handleResolveWordChoice` already routes through `saveItemWithCategory`, whose SHOPPING branch (325-328) is live when called from the shopping view, s

### 23. [medium] Category move persists to DB but the UI never re-fetches when the same submit also yields an uncertain item or an ambiguous removal
**תיקון:** Make the refresh depend on `movedCount`, not on which status message wins. In `handleUpdate`, right after the certain-items save loop (line 435), add an unconditional refresh, and drop the now-redundant call from the `movedCount` arm:

        // ביצוע השמירות
        for (const item of certainItems) await saveItemWithCategory(item);
        if (movedCount > 0) await fetchData();

and change lines 452-454 to:

        } else if (movedCount > 0) {
          showStatus(`✅ ${movedCount} פריטים הועברו!`, true);

Placing the `await fetchData()` before the state setters keeps the existing ordering semantics (fetchData itself pushes uncertain-category DB rows into `pendingItems`, so it must not run after line 450's `setPendingItems`). Optionally also mention the move in the uncertain-item message, but the refresh is the load-bearing part.

### 24. [low] Equipment "item not found" warning is always overwritten by "✅ עודכן!"
**תיקון:** Accumulate results in the loop and emit exactly one status afterwards. In handleEquipmentUpdate:

- Before the loop (after line 1066): `let removed = 0; let added = 0; const notFound: string[] = [];`
- Line 1073-1077 becomes:
```
for (const m of matches) {
  setEquipmentItems(prev => prev.filter(i => i.id !== m.id));
  await supabase.from('equipment_items').delete().eq('id', m.id);
  removed++;
}
if (matches.length === 0) notFound.push(name);
```
- Line 1087: after `if (inserted) setEquipmentItems(prev => [...prev, ...inserted]);` add `added += inserted?.length ?? 0;`
- Replace line 1090 with:
```
if (notFound.length > 0 && removed === 0 && added === 0) {
  showStatus(`לא מצאתי ${notFound.map(n => `"${n}"`).join(', ')} ברשימה`, true);
} else if (notFound.length > 0) {
  showStatus(`✅ עודכן! (לא נמצא: ${notFound.join(', ')})`, true);
} else {
  showStatus('✅ עודכן!', true);
}
```
That is the minimal fix and keeps the success path unchanged. Apply the same "one status after the loop" shape to the inventory not-found at line 392 vs the final else at 458. The substring-match confirmation (prefer an exact match when one exists, otherwise route multi-matches through the existing disambig

### 25. [low] autoFillRowCategory races with form submission and pre-fills a category on the cleared form
**תיקון:** Make the async write self-validating — capture the name before the await and only apply the guess if the target row still holds it. In `autoFillRowCategory` (page.tsx:553-556) replace the body of `if (guess) { ... }` with:

      if (guess) {
        let applied = false;
        setQuickAddRows(prev => {
          if (prev[idx]?.name.trim() !== name || prev[idx].category) return prev;
          applied = true;
          return prev.map((r, i) => i === idx ? { ...r, category: guess } : r);
        });
        if (applied) setCategories(prev => prev.includes(guess) ? prev : [...prev, guess]);
      }

That drops any guess whose row was cleared (or retyped) while the request was in flight, and stops `setCategories` from appending a category nothing uses. Note the updater must stay pure-ish for React 18 double-invocation in dev: computing `applied` from the same condition twice is idempotent here, but if that bothers you, compute the decision from a ref/state read instead and keep the setter pure.

### 26. [low] Task dependency picker has no cycle guard — mutual dependency deadlocks both tasks' status buttons
**תיקון:** Add a reachability check and use it in the edit picker only (the new-task picker needs nothing). Next to getBlockingTask in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:

  const reachesTask = (candidate: Task, targetId?: string): boolean => {
    if (!targetId) return false;
    const seen = new Set<string>();
    let cur: Task | undefined = candidate;
    while (cur?.depends_on_task_id) {
      const nextId = cur.depends_on_task_id;
      if (nextId === targetId) return true;
      if (seen.has(nextId)) return false;
      seen.add(nextId);
      cur = tasks.find(t => t.id === nextId);
    }
    return false;
  };

Then at line 1868 change the filter to:

  {tasks.filter(t => t.id !== task.id && t.status !== 'סיימתי' && !reachesTask(t, task.id)).map(t => (

The `seen` set makes it safe against a cycle already stored in the database from before the fix. Optionally also short-circuit in saveEditTask for the same condition, so a stale open editor cannot write a cycle after another device changed the graph.

### 27. [medium] setState inside a .filter() predicate appends to pendingItems on every fetchData, so classification cards multiply
**תיקון:** Two edits in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx.

1. In fetchData (lines 161-169), partition without a setter in the predicate and REPLACE the DB-sourced pending list instead of appending (keeping any locally-created pending items, which have no id):

```ts
    if (invRes.data) {
      const isUncertain = (i: any) => i.category === 'uncertain' || i.category === 'לא ידוע';
      const uncertain = (invRes.data as any[]).filter(isUncertain);
      const cleanInv = (invRes.data as any[]).filter(i => !isUncertain(i));
      setPendingItems(prev => [
        ...prev.filter(p => !p.id),                       // locally parsed, not yet in DB
        ...uncertain.map(i => ({ ...i, item_name: i.item_name || i.name || 'פריט', needs_classification: true })),
      ]);
      setInventory(cleanInv);
    }
```

2. Make resolution actually clear the row, otherwise the card returns on the next fetch. In handleResolvePending (465-469), update the existing row by id when there is one:

```ts
  const handleResolvePending = async (item: Item, selectedCategory: string) => {
    setPendingItems(prev => prev.filter(p => (item.id ? p.id !== item.id : p.item_name !== 

### 28. [high] Resolving a pending classification inserts a duplicate inventory row and the card returns forever
**תיקון:** In saveItemWithCategory, branch on the row id before falling back to the name lookup (`item.id` is present only for rows that came from the DB — the parse API never returns an id):

    if (activeView === 'INVENTORY') {
      if (item.id) {
        await supabase.from('inventory_items')
          .update({ category: cat, quantity: Math.max(1, item.quantity || 1) })
          .eq('id', item.id);
      } else {
        let existing = inventory.find(i => i.item_name === name);
        ... existing update/insert logic unchanged ...
      }
    }

Do not route the id case through the `existing.quantity + (item.quantity || 1)` path — for a pending DB row item.quantity is already the stored quantity, so adding would double it. Apply the same id-based update in the onIgnore handler (lines 1258-1261) so dismissing writes category 'כללי' to that row instead of leaving it uncertain; otherwise the card still reappears on the next fetchData. Worth also deduping the pendingItems append at line 163 by id, since repeated fetchData calls currently stack identical cards.

### 29. [medium] handleUpdate's sequential save loop reads stale `inventory`/`categories` closure state, so duplicate names in one submission create duplicate rows (and duplicate category_order rows)
**תיקון:** Two small changes in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, both confined to handleUpdate + saveItemWithCategory:

1. Collapse same-name items before writing. Replace the loop at line 435 with an aggregation pass so the loop never sees one name twice:

const mergedCertain = Array.from(
  certainItems.reduce((m, it) => {
    const key = (it.name || it.item_name || '').trim().toLowerCase();
    const prev = m.get(key);
    if (prev) prev.quantity = (prev.quantity || 1) + (it.quantity || 1);
    else m.set(key, { ...it, quantity: it.quantity || 1 });
    return m;
  }, new Map<string, any>()).values()
);
const createdCats = new Set<string>(categories);
for (const item of mergedCertain) await saveItemWithCategory(item, createdCats);

2. Stop saveItemWithCategory from re-inserting a category it already created in this batch. Give it the shared set instead of reading the captured `categories`:

const saveItemWithCategory = async (item: any, createdCats?: Set<string>) => {
  ...
  const knownCats = createdCats ?? new Set(categories);
  if (cat !== 'כללי' && !knownCats.has(cat)) {
    await supabase.from('category_order').insert([{ category_name: cat, s

### 30. [low] Stale `categories` closure lets the same new category be INSERTed into category_order once per item in a loop
**תיקון:** Hold the known set in a ref, which is never stale inside a handler, and route all five sites through one helper.

1. Next to the state at page.tsx:85 add:
```ts
const knownCategoriesRef = useRef<Set<string>>(new Set());
```
2. Keep it in sync (and dedupe state) at page.tsx:171:
```ts
if (catsRes.data) {
  const names = Array.from(new Set(catsRes.data.map(c => c.category_name)));
  knownCategoriesRef.current = new Set(names);
  setCategories(names);
}
```
3. Add one helper:
```ts
const ensureCategory = async (cat: string) => {
  if (!cat || cat === 'כללי' || knownCategoriesRef.current.has(cat)) return;
  knownCategoriesRef.current.add(cat);            // claim it before awaiting
  await supabase.from('category_order').insert([{ category_name: cat, sort_order: 99 }]);
  setCategories(prev => Array.from(new Set([...prev, cat])));
};
```
4. Replace each of the five guard blocks (310-313, 343-347, 649-652, 704-707, 796-799) with `await ensureCategory(cat)` (`nc` / `target` respectively). Adding to the ref before the `await` also closes the concurrent-submit window, which a Set seeded per handler would not.

### 31. [medium] pendingItems cards keyed by array index while ClassificationCard holds internal customCat state — typed category leaks onto the next item
**תיקון:** One-line change at /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:1254 — key by the same identity the removals use:

```
key={item.item_name}
```

instead of `key={`pending-${idx}`}` (the `idx` binding in the map can then be dropped). `item_name` is already the de-facto identity of a pending item: both removal paths filter on `p.item_name !== item.item_name`, so two entries can never meaningfully share a name.

If a belt-and-braces guard inside the component is also wanted, reset on identity change in ClassificationCard — but it is not needed once the key is stable, and the key fix is the minimal correct one.

The `dis-${idx}` / `wc-${idx}` keys need no change for correctness; switch them to `task.originalName` only as cleanup.

### 32. [medium] Every mutation fires an unsequenced 8-query fetchData; the last burst to answer wins, resurrecting deleted rows
**תיקון:** Two small edits in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, no refactor needed.

(a) Sequence fetchData so stale bursts cannot write. Add a ref next to statusTimeoutRef (line 140):

    const fetchSeqRef = useRef(0);

and at the top / after the await in fetchData (lines 148-159):

    const fetchData = async () => {
      const seq = ++fetchSeqRef.current;
      const [invRes, shopRes, ...] = await Promise.all([...]);
      if (seq !== fetchSeqRef.current) return; // a newer refetch already landed
      ... existing setters unchanged ...

One line in, one line out; every one of the 16 call sites keeps working and only the newest response is allowed to paint.

(b) Stop issuing one burst per saved item: delete the trailing `fetchData();` at line 329 inside saveItemWithCategory and refetch once after the batch loop, i.e. change line 435 to

    for (const item of certainItems) await saveItemWithCategory(item);
    if (certainItems.length > 0) fetchData();

Check the other saveItemWithCategory callers (the pending-classification and word-choice handlers) still trigger one fetchData after their own loop, since they lose the per-item one.

The AbortCont

### 33. [medium] Overlapping task completions collide on single-slot poof/celebration state and uncleared timers (the unmount half of the claim is a misreading)
**תיקון:** Track the timers in a ref, and when a second completion starts, FLUSH the previous task's pending commit instead of merely clearing it (dropping that 1500ms timer would leave the previous task stuck in the active list even though its DB row was already updated). Also remount the overlay so the CSS animations restart.

In page.tsx, near the state declarations:

    const poofRef = useRef<{ timers: number[]; commit: (() => void) | null }>({ timers: [], commit: null });
    useEffect(() => () => { poofRef.current.timers.forEach(clearTimeout); }, []);

Replace the 'סיימתי' branch body (page.tsx:995-1007) with:

    poofRef.current.timers.forEach(clearTimeout);
    poofRef.current.commit?.();            // flush the previous task's status commit
    poofRef.current.timers = [];
    const commit = () => setTasks(prev => prev.map(t => t.id === id ? { ...t, status: newStatus } : t));
    poofRef.current.commit = commit;
    setPoofingTaskId(id);
    setCelebration({ ...buildCelebration(), runId: Date.now() });
    supabase.from('tasks').update({ status: newStatus }).eq('id', id).then(() => {});
    poofRef.current.timers = [
      window.setTimeout(() => { poofRef.current.commit = null; co

### 34. [medium] Swipe-to-delete reads dragX from state in onTouchEnd; a fast flick past the threshold snaps back instead of deleting
**תיקון:** Mirror the distance into a ref and make onTouchEnd branch on the ref; keep the state copy only for the transform. In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:

1. Add next to the existing refs (after line 2370):

    const dragXRef = useRef(0);

2. In onTouchStart (after line 2376) reset it:

    dragXRef.current = 0;

3. In onTouchMove, replace line 2390 with:

    const next = Math.max(0, dx);
    dragXRef.current = next;
    setDragX(next);

4. In onTouchEnd, branch on the ref and keep it in sync with the two state writes:

    if (dragXRef.current >= SWIPE_THRESHOLD) {
      dragXRef.current = 420;
      setDragX(420);
      window.setTimeout(() => onDelete(item, true), 160);
      return;
    }
    dragXRef.current = 0;
    setDragX(0);

Optional hardening in the same edit: onTouchMove's guard `if (!dragging) return;` (line 2380) also reads state, so a `draggingRef` would make the first move of a gesture robust too. Lower risk, since touchstart is a discrete event whose SyncLane update flushes in the microtask before the next listener — but it is the same class of bug and cheap to close while you are in the function.

### 35. [medium] `input` and `status` are shared by the INVENTORY and TEMPLATES free-text forms and never reset on view change
**תיקון:** Clear the shared buffer and status in `changeView` (page.tsx:228-230):

const changeView = (view: typeof activeView) => {
  setActiveView(view);
  setIsMenuOpen(false);
  setIsSearchOpen(false);
  setSearchTerm('');
  setAddedSummary([]);
  setInput('');
  if (statusTimeoutRef.current) { clearTimeout(statusTimeoutRef.current); statusTimeoutRef.current = null; }
  setStatus('');
};

That is the minimal fix. The cleaner follow-up, if drafts should be preserved per view, is to give the two forms separate state (e.g. `input` for inventory/shopping and a new `equipInput` for the equipment textarea at line 1932, with `handleEquipmentUpdate` reading and clearing that one), since the two textareas feed different parser contexts and different tables.

### 36. [medium] handleUpdate replaces pendingItems instead of merging, silently dropping unresolved classification cards
**תיקון:** Make both writers merge by identity.

At src/app/page.tsx:450 replace the overwrite with an append that skips items already pending:

    } else if (uncertainItems.length > 0) {
      const incoming = uncertainItems.map((i: any) => ({ ...i, item_name: i.name || i.item_name || 'פריט' }));
      setPendingItems(prev => {
        const seen = new Set(prev.map(p => p.id || p.item_name));
        return [...prev, ...incoming.filter(i => !seen.has(i.id || i.item_name))];
      });

At src/app/page.tsx:163 guard the append so repeated fetchData calls do not stack duplicates:

      setPendingItems(prev => prev.some(p => (p.id || p.item_name) === (i.id || i.item_name))
        ? prev
        : [...prev, { ...i, needs_classification: true }]);

Removal stays where it already is — handleResolvePending (line 467) and the onIgnore filter (line 1259) — which is correct: entries clear only when actually resolved.

### 37. [low] Theme and font are applied only after hydration, causing a visible flash of the default palette
**תיקון:** Move the two attributes off the React-rendered <main> and onto <html>, set by a pre-paint inline script; keep the effect only to sync React state for the settings UI highlight. Four small edits:

1) src/app/layout.tsx — set the attributes before first paint, and silence the hydration attribute diff on <html>:

    <html lang="he" dir="rtl" suppressHydrationWarning>
      <head>
        <script
          dangerouslySetInnerHTML={{
            __html: `try{var d=document.documentElement,t=localStorage.getItem('neo_theme'),f=localStorage.getItem('neo_font');if(t)d.dataset.theme=t;if(f)d.dataset.font=f;}catch(e){}`,
          }}
        />
      </head>

2) src/app/globals.css — let the same rules match on the root element (keep the main selectors so nothing else breaks):

    :root[data-theme="lyra"], main[data-theme="lyra"] { ... }
    (same for ocean / sunset / mono, and for the five [data-font="…"] rules)

3) src/app/page.tsx:1164 — stop emitting the default attributes on <main>, otherwise the closer ancestor wins the cascade and the root attribute has no effect:

    <main className="min-h-screen bg-[var(--c-bg)] pb-20 text-[var(--c-ink)] transition-colors duration-500" dir="rtl">

### 38. [medium] Swipe reveal label sits on the RTL flex-start (right) edge while the card uncovers the left strip, so "החלק למחיקה" stays hidden for the whole gesture
**תיקון:** One-word change at /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:2408 — add `justify-end` to the reveal layer so it aligns to the flex main-END, which under `dir="rtl"` is the LEFT edge, i.e. exactly the strip the rightward-sliding card uncovers:

    className={`absolute inset-0 flex items-center justify-end px-6 rounded-[1.5rem] transition-colors ${armed ? 'bg-red-500' : 'bg-red-300'}`}

`justify-end` is logical in Tailwind, so it keeps working if the app is ever served LTR (the label would then follow to the correct side automatically). No other change is needed for this finding.

If the sibling finding about swipe direction is fixed instead (card slides toward the inline-end, i.e. leftward in RTL, uncovering the right strip), then `flex-start` as written is already correct and `justify-end` must NOT be added — so fix at most one of the two, and verify on a real touch device that the red band and the label appear on the same side.

### 39. [medium] ShoppingCard has no non-touch delete path; swipe delete also bypasses the confirm() every other delete uses
**תיקון:** Add the visible delete button to ShoppingCard so a non-touch user has a path, and let it go through the normal confirm. In ShoppingCard's header row (page.tsx:2431, the flex row that opens with the "📦 הוסף למלאי" button), add alongside it:

  <button onClick={() => onDelete(item)} className="w-9 h-9 rounded-full bg-slate-100 text-slate-400 hover:bg-red-100 hover:text-red-600 font-black text-xs pointer-events-auto shadow-sm transition-colors active:scale-90" title="מחק לצמיתות">🗑️</button>

Calling onDelete with one argument leaves skipConfirm at its false default, so this path hits the confirm() at page.tsx:277 like every other delete. Then fix the now-wrong comment at page.tsx:2364 ("replaces the delete button" → "in addition to the delete button").

Do NOT simply drop the `true` at line 2397: on cancel, dragX is already 420 and the card would stay slid off-screen. If a prompt on the swipe is wanted too, gate it before the slide-off inside onTouchEnd and reset on cancel:

  if (dragX >= SWIPE_THRESHOLD) {
    if (!confirm(`למחוק לצמיתות את "${item.item_name}"?`)) { setDragX(0); return; }
    setDragX(420);
    window.setTimeout(() => onDelete(item, true), 160);
    return;
  }

Le

### 40. [low] Delete button trapped inside the stepper's dir="ltr" island, landing between the item name and "+"
**תיקון:** Minimal change in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx: close the LTR island right after the `+` button (line 2337) and re-emit the delete button as the next sibling in the RTL parent row, unchanged otherwise. I.e. move the `</div>` currently on line 2339 to just after line 2337, so the structure becomes:

<div className="flex items-center gap-2" dir="ltr">
  ... − / qty / + ...
</div>
<button onClick={() => onDelete(item)} className="w-9 h-9 rounded-full bg-slate-100 text-slate-400 hover:bg-red-100 hover:text-red-600 font-black text-xs pointer-events-auto shadow-sm transition-colors active:scale-90" title="מחק לצמיתות">🗑️</button>

The row then has three children; the name keeps `flex-1` so it absorbs the free space and `justify-between` produces no stray gap. The stepper still reads `− N +`, and 🗑️ moves to the card's inline-end (left), matching equipment/task/completed-task cards. Optionally add `flex-shrink-0` to the delete button to keep its 36px target on narrow phones.

### 41. [low] Task target date rendered as raw ISO yyyy-mm-dd on the task chip, while the same field is formatted two other ways elsewhere
**תיקון:** Add one helper next to `today` (after page.tsx:75) and use it in all three places so the app speaks one date format. Note `new Date(iso)` on a bare `yyyy-mm-dd` parses as UTC and can shift a day in Israel time, hence the `T00:00:00` suffix, plus a guard for an empty value:

```ts
const fmtDate = (iso?: string) => {
  if (!iso) return '';
  const d = new Date(`${iso}T00:00:00`);
  return isNaN(d.getTime()) ? iso : d.toLocaleDateString('he-IL');
};
```

Then:
1. page.tsx:1812 — `📅 {fmtDate(task.target_date)}`
2. page.tsx:887-888 — delete the manual split and use `const formattedDate = fmtDate(t.target_date);` (this also removes the crash-on-null risk in `t.target_date.split('-')`)
3. page.tsx:2323 — `{fmtDate(item.updated_at.split('T')[0])}`, or leave as-is since it already uses `he-IL` and now matches

If DD/MM/YYYY is preferred over he-IL's `27.8.2026`, keep the manual build inside `fmtDate` with zero-padding instead — the point is that one function owns the format. Do not format at 1757/1852-1853: those are `<input type="date">` and require the raw ISO value.

### 42. [low] Template item bullets use mr-1, which in RTL puts the gap on the wrong side of the bullet
**תיקון:** Change `mr-1` to `me-1` (logical `margin-inline-end`) on the three bullet spans at /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:2147, :2159, :2170:

    <span className="text-[var(--c-primary)] me-1">•</span> {it.item_name}

`me-1` resolves to margin-right in LTR and margin-left in RTL, so the gap always falls between the bullet and the item name. No other file needs touching; these are the only three `mr-1` occurrences in page.tsx.

### 43. [medium] Swipe-to-delete card declares no touch-action, so the browser keeps ownership of the touch and fights the JS drag
**תיקון:** Two one-line changes in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx — no globals.css edit needed, Tailwind v4 already ships the utility.

1. Give the browser only the vertical axis. Add `touch-pan-y` (compiles to `touch-action: pan-y`) to the dragged div's className at line 2421:
`className={`relative touch-pan-y flex flex-col gap-3 bg-white p-4 ...`}`

2. Stop a cancelled gesture from deleting. Split the cancel handler instead of aliasing it to onTouchEnd (line 2419):
```
onTouchCancel={() => { setDragging(false); axisLocked.current = 'none'; setDragX(0); }}
```

The JS axis lock at 2384-2389 stays as-is. Do not claim this also fixes the iOS standalone edge-back swipe — it does not; if that case matters, it needs a separate non-passive touchstart listener via a ref, or a left-edge dead zone in onTouchStart (ignore starts with clientX < ~24).

### 44. [no] Shopping-list delete is touch-swipe-only (no button, no pointer handler) and fires with no confirm or undo
**תיקון:** Two small edits inside `ShoppingCard`, both in `/Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx`:

1. Restore a visible delete control (fixes the non-touch case; mirrors `InventoryCard`'s existing button). Next to the `📦 הוסף למלאי` button at line ~2424 add:

```jsx
<button onClick={() => onDelete(item)} className="w-9 h-9 rounded-full bg-slate-100 text-slate-400 hover:bg-red-100 hover:text-red-600 font-black text-xs pointer-events-auto shadow-sm transition-colors active:scale-90" title="מחק לצמיתות">🗑️</button>
```

Note it calls `onDelete(item)` without the second arg, so `skipConfirm` stays `false` and the existing `confirm()` runs — no change needed in `deleteShoppingItem`.

2. Harden the gesture so a diagonal scroll can't commit. Raise the threshold and require the axis lock to still be `'x'` at release — `onTouchEnd` currently resets `axisLocked.current = 'none'` before testing `dragX`, so the lock is never consulted:

```js
  const SWIPE_THRESHOLD = 150;
  ...
  const onTouchEnd = () => {
    setDragging(false);
    const wasX = axisLocked.current === 'x';
    axisLocked.current = 'none';
    if (wasX && dragX >= SWIPE_THRESHOLD) { ... }
```

If an

### 45. [no] Swipe-to-delete reveal label is rendered at the right edge (RTL flex-start) while the card slides right, so the text stays hidden under the card for the whole gesture
**תיקון:** Minimal fix — put the label on the side the card actually uncovers, by adding `justify-end` to the background flex row at page.tsx:2408 (under dir="rtl", `justify-content: flex-end` resolves to the left/physical edge that the rightward-sliding card reveals):

    className={`absolute inset-0 flex items-center justify-end px-4 rounded-[1.5rem] transition-colors ${armed ? 'bg-red-500' : 'bg-red-300'}`}

`px-6` -> `px-4` is optional but helps: with 24px padding the text only starts appearing at dragX > 24 and is fully legible near dragX ≈ 24 + text width (~120px, i.e. right at the 110px threshold); 16px brings it in slightly earlier.

Optional polish, same line 2409 — fade the layer in with the drag instead of the hard 0/1 toggle:

    style={{ opacity: Math.min(1, dragX / 40) }}

No other change needed; the transform, threshold, and delete path at lines 2390-2401 are correct as written.

### 46. [medium] 28 of 33 form controls render below 16px, so every field tap auto-zooms Mobile Safari in this standalone PWA
**תיקון:** One rule in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/globals.css. Append it at the end of the file, outside any `@layer`:

@media (max-width: 768px) {
  input, select, textarea { font-size: 16px; }
}

Why this placement and not `@layer base`: globals.css starts with `@import "tailwindcss"`, which declares the `theme, base, components, utilities` layers. Unlayered CSS outranks every layered rule regardless of specificity, so an unlayered rule at the end of the file beats the `text-sm` / `text-xs` / `text-[10px]` utilities. The same rule written inside `@layer base` would lose to the utilities layer and the bug would survive — this is the mistake to avoid.

One side effect to accept or patch: the two deliberately larger inputs at page.tsx:2316 and page.tsx:2440-area (`text-lg`, 18px) get clamped to 16px. Both stay at or above the threshold so neither zooms; if the 18px matters visually, follow the guard with `input.text-lg { font-size: 18px; }`.

The two `text-[10px]` inline category inputs (page.tsx:2348, 2440) will visibly grow from 10px to 16px inside their `w-[90px]` / `w-[80px]` boxes. Widen those two to about `w-[130px]` in the same edit so the placehol

### 47. [medium] Inventory quantity row packs three 36px controls 8px apart, with permanent delete next to the +/- buttons
**תיקון:** Minimal, low-risk, no new interaction model needed — two edits inside InventoryCard (/Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx):

1. Enlarge the increment cluster and drop the delete button from it. Replace lines 2329-2339 with the same block minus the 🗑️ button, sized up:

  <div className="flex items-center gap-3" dir="ltr">
    <button onClick={onMinus} aria-label="הפחת יחידה" className="w-11 h-11 rounded-full bg-rose-100 text-[var(--c-primary)] font-black pointer-events-auto shadow-sm hover:bg-rose-200 transition-colors active:scale-90">-</button>
    <div className="relative flex items-center justify-center min-w-[44px] h-11">
      <select aria-label="קבע כמות" ... >   {/* unchanged */}
      <span ...>{item.quantity || 0}</span>
    </div>
    <button onClick={onPlus} aria-label="הוסף יחידה" className="w-11 h-11 rounded-full bg-emerald-100 text-emerald-600 font-black pointer-events-auto shadow-sm hover:bg-emerald-200 transition-colors active:scale-90">+</button>
  </div>

2. Re-home the delete into the existing chip row at line 2342 (`<div className="flex items-center gap-2 flex-wrap text-[10px] font-black">`), at its end, with a visible labe

### 48. [medium] Quantity picker is an invisible full-bleed select with no affordance, and its option list stops at 20 while quantity is unbounded
**תיקון:** Two small edits in the same block, page.tsx:2331-2335. Keep the native picker (it is the right control for "jump to an exact number"), but give it a real affordance, a 44px target, and a range that always covers the current value.

Add above the `return` in `InventoryCard` (after line 2309):

```tsx
const qty = Math.floor(item.quantity || 0);
const maxOpt = Math.max(20, qty + 5);
```

Then replace lines 2331-2336 with:

```tsx
<div className="relative flex items-center justify-center min-w-[44px] h-11 rounded-xl border border-dashed border-slate-300 bg-slate-50">
  <select
    value={qty}
    onChange={(e) => updateExactQuantity(item, Number(e.target.value))}
    aria-label={`כמות מדויקת של ${item.item_name || 'הפריט'}`}
    className="absolute inset-0 w-full h-full opacity-0 cursor-pointer pointer-events-auto"
  >
    {Array.from({ length: maxOpt + 1 }, (_, num) => <option key={num} value={num}>{num}</option>)}
  </select>
  <span className={`text-xl font-black pointer-events-none ${isLow ? 'text-[var(--c-primary)]' : 'text-slate-700'}`}>{item.quantity || 0}</span>
  <span className="absolute bottom-0 right-0.5 text-[8px] text-slate-400 pointer-events-none">▾</span>
</div>
```

Wh

### 49. [medium] Inline category editor's confirm/cancel and chips are 10px targets with no padding
**תיקון:** Minimal, class-only, no behaviour change. Two spots.

InventoryCard (page.tsx:2344-2351):
- container: `flex items-center gap-1 flex-wrap flex-1` → `flex items-center gap-2 flex-wrap flex-1`
- chips: `px-2 py-0.5 rounded-lg text-[10px] font-bold` → `px-3 min-h-9 rounded-lg text-sm font-bold inline-flex items-center`
- input: `text-[10px] font-bold w-[90px]` → `text-base font-bold w-[120px] min-h-9` (16px kills the Safari focus-zoom)
- ✅ button: `className="text-[10px] font-black text-green-600 pointer-events-auto"` → `className="min-h-11 min-w-11 inline-flex items-center justify-center text-base font-black text-green-600 pointer-events-auto"`
- ✕ button: same treatment with `text-slate-400`

ShoppingCard (page.tsx:2436-2443): identical edits, keeping the rose palette — chips `bg-rose-50 text-[var(--c-primary)] px-3 min-h-9 rounded-lg text-sm font-bold inline-flex items-center`, input `text-base font-bold w-[110px] min-h-9`, and the two bare buttons given the same `min-h-11 min-w-11 inline-flex items-center justify-center text-base`.

Note that in ShoppingCard the editor lives in a `flex-1` column beside the 📦 button, so after enlarging the chips give that row room: on the wrapper a

### 50. [medium] Task chips advertise "jump to this field" but only toggle a faint ring — no scroll, no picker, and no focus at all on a re-tap
**תיקון:** Minimal, keeps native controls — replace `autoFocus` on the three targets with one ref-driven effect so it also fires on a re-tap:

1. Add refs in the task-card render: `const focusRef = useRef<HTMLElement|null>(null)` at component level keyed per open task (or a single `editFocusRef` on the page component, since only one task edits at a time), and set `ref={editTaskFocus === 'date' ? editFocusRef : null}` on the date input, same pattern for the two selects and the title input.

2. Add an effect that runs on both open and focus change:
`useEffect(() => { const el = editFocusRef.current; if (!editingTaskId || !el) return; el.scrollIntoView({ block: 'center', behavior: 'smooth' }); el.focus({ preventScroll: true }); if (editTaskFocus === 'date') { try { (el as HTMLInputElement & { showPicker?: () => void }).showPicker?.(); } catch {} } }, [editingTaskId, editTaskFocus]);`
`scrollIntoView({block:'center'})` sidesteps the sticky header without a magic offset; the `showPicker` call is wrapped because it throws without transient activation and does not exist on iOS Safari — it is a desktop bonus, not the mechanism.

3. Make the cue visible: swap `ring-[var(--c-ring)]` for `ring-[var(--c-

### 51. [low] Icon-only buttons carry their label only in a title attribute, which never appears on touch
**תיקון:** Add `aria-label` alongside the existing `title` on the genuinely icon-only buttons, and nothing else. One attribute per line, no markup or layout change:

L1219 → `aria-label="חפש"`
L1220 → `aria-label="רענן עמוד"`
L1337 → `aria-label="נקה חיפוש"`
L1578 → `aria-label="סגור"`
L1686 → `aria-label={`שנה שם קטגוריה ${cat}`}`
L1687 → `aria-label={`מחק קטגוריה ${cat}`}`
L2128 → `aria-label={`מחק רשימה ${tpl.name}`}`
L2338 → `aria-label={`מחק לצמיתות ${item.item_name}`}`
L1976 → add both `title="מחק לצמיתות"` and `aria-label={`מחק לצמיתות ${item.item_name}`}` (currently has neither)
L2349 → `title="שמור קטגוריה" aria-label="שמור קטגוריה"`
L2350 → `title="בטל" aria-label="בטל עריכת קטגוריה"`

Skip L1450, L1942, L2121, L2355 — they already render visible Hebrew labels. Skip the proposed visible captions on the 🗑️ controls: the confirm dialogs at L257/277/579/769/1019/1976 already name the target item before anything is deleted.

### 52. [medium] Tap-to-edit is bound to selectable <span> text with no user-select/touch-callout suppression, so a held tap on iOS starts text selection instead of entering edit mode
**תיקון:** Two steps; step 1 alone fixes the reported failure.

1. Add one utility to `/Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/globals.css` (e.g. after the `::selection` rule on line 98):

```css
/* Tap targets that are plain text: never let iOS start a selection/callout on them */
.tap-edit {
  -webkit-user-select: none;
  user-select: none;
  -webkit-touch-callout: none;
}
```

Then add `tap-edit` to the className of the four elements: `page.tsx:1216` (h1), `1975`, `2320`, `2432`. With `user-select: none` the long-press selection gesture never starts, so the click fires on release regardless of press duration — this is the whole bug.

2. Optional but worth doing while there, to match the existing pattern at `page.tsx:2445`: change the three item-name spans (1975, 2320, 2432) to `<button type="button" …>` keeping the same classes plus `text-right bg-transparent border-none p-0` and adding an `active:` cue (e.g. `active:text-[var(--c-primary)]`), since `hover:underline` gives no feedback on touch. The h1 can stay an `<h1>` with the `tap-edit` class — HOME is also reachable from the ☰ menu, so it is not a sole-path control.

Skip the "44px minimum height" part of the 

### 53. [medium] Programmatic navigation to wa.me exits the installed PWA with no way back
**תיקון:** Open the share target out-of-window so the PWA window survives. Minimal edit in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, replacing line 906:

    if (sendToWhatsApp) {
      window.open(buildWhatsAppLink(taskCopy), '_blank', 'noopener,noreferrer');
    }

One caveat worth handling in the same edit: the `await supabase...insert` on line 899 consumes the click's user-activation, and Safari can block a window.open that happens after an await. The robust version opens the tab synchronously at click time and only fills in the URL after the insert resolves (no `noopener`, since that makes window.open return null and you need the handle):

  const persistNewTask = async (sendToWhatsApp: boolean) => {
    if (!newTask.title) return;
    const shareWin = sendToWhatsApp ? window.open('', '_blank') : null;
    const { error } = await supabase.from('tasks').insert([newTask]);
    if (error) { shareWin?.close(); return; }
    const taskCopy = { ...newTask };
    ...
    if (shareWin) shareWin.location.href = buildWhatsAppLink(taskCopy);
  };

Optional improvement on top: prefer `navigator.share({ text })` when available — on iOS that keeps the user inside the 

### 54. [medium] Header title wraps to two lines at 375px on every non-HOME view, changing sticky-header height
**תיקון:** Three token edits in the header row of /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx (no CSS changes needed):

1. line 1217 — responsive size + never wrap:
   className="text-xl sm:text-2xl font-black cursor-pointer drop-shadow-md truncate min-w-0"
   (truncate sets overflow:hidden, which zeroes the flex automatic minimum size, so the h1 ellipsizes instead of wrapping even at 320px.)

2. line 1218 — keep the controls at full size:
   className="flex items-center gap-2 shrink-0"

3. line 1223 — icon-only menu under sm:
   <span>☰</span><span className="hidden sm:inline">תפריט</span>

Measured effect: at 20px the title is ~144.8px and the controls drop to ~130px, so 375px (343 available) and 320px (288 available) both fit on one line with the 🔍 present, and header height is identical across HOME and the other views. Edits 1+2 alone already stop the wrap and the height change at 375/360; edit 3 is what buys the 320px case.

### 55. [medium for the duplicate/dead search field] Header 🔍 opens a second field bound to the same searchTerm — duplicate on Inventory/Shopping, dead control on Tasks/Templates/Settings
**תיקון:** Delete the header search entirely — there is no view where it does something the page does not already do better.

1. page.tsx:1219 — remove the toggle:
   `{activeView !== 'HOME' && <button onClick={() => setIsSearchOpen(!isSearchOpen)} ...>🔍</button>}`
2. page.tsx:1237-1241 — remove the whole `{isSearchOpen && activeView !== 'HOME' && ( ... )}` block.
3. page.tsx:82 — drop `isSearchOpen` state, and its reset in `changeView` (page.tsx:229). Keep `setSearchTerm('')` there.

Do NOT gate 🔍 to Tasks as the original fix proposed: `searchTerm` is not wired into the tasks list (page.tsx:1147, 1766), so that would leave an autoFocused field that filters nothing. If a tasks search is wanted, that is a separate change (add `.filter(t => (t.title || '').includes(searchTerm))` to the tasks render plus an in-page field matching Inventory/Shopping).

Optional, unrelated to the above: bump the clear button at page.tsx:1337 from `w-7 h-7` to `w-9 h-9` (or wrap in a 44px hit area with `touch-manipulation`) for tap comfort. Not a bug fix — the button already receives the tap.

### 56. [medium] Packing-list rows: 28px pack toggle vs. large name area that opens an un-cancellable rename
**תיקון:** Minimal, contained to the two equipment rows in `page.tsx` (lines 1964-1977 and 1997-2005); do not touch the shared rename convention in InventoryCard/ShoppingCard.

1. Make checkbox + name one toggle target, and move rename to an explicit control. Replace the `<div className="flex items-center gap-3 flex-1">` wrapper's contents (non-editing branch) with a toggle button that spans the row, plus a small ✏️ button:

    <button
      onClick={() => togglePacked(item)}
      role="checkbox"
      aria-checked={false}
      aria-label={`סמן כנארז: ${item.item_name}`}
      className="flex items-center gap-3 flex-1 min-h-[44px] text-right pointer-events-auto"
    >
      <span className="w-7 h-7 rounded-lg border-2 border-sky-300 bg-white flex-shrink-0" aria-hidden="true" />
      <span className="font-bold text-slate-800 flex-1">{item.item_name}</span>
    </button>
    <button onClick={() => { setEditingId(item.id!); setEditNameValue(item.item_name); }} aria-label="שנה שם" className="text-slate-300 hover:text-[var(--c-primary)] p-3 pointer-events-auto">✏️</button>

   Keep the existing `editingId === item.id` input branch untouched.

2. Grow the delete hit box: change `... transition-

### 57. [low] Completed-task block is permanently dimmed on touch devices — its full opacity is behind a hover variant Tailwind v4 gates out
**תיקון:** Remove the hover dependency at /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:1908 — drop the group opacity entirely, since the completed cards are already de-emphasised by `bg-slate-100` + `line-through` + `text-slate-500`:

    - <div className="space-y-4 opacity-70 hover:opacity-100 transition-opacity">
    + <div className="space-y-4">

If some visual dimming is still wanted, use a value that keeps the controls readable (`opacity-90`) instead of 70, and darken the 10px label in CompletedTaskCard (page.tsx:2295) from `text-slate-400` to `text-slate-600` so the restore row clears AA on its own without depending on any pointer state.

### 58. [high] shopping_history upsert targets a column with no unique index — every history write returns 400 (42P10), silently
**תיקון:** Code-only, no schema change. `last_used` is written but read nowhere (grep for it in src/ hits only the line that writes it, and fetchData selects `item_name` alone), so the merge on repeat names buys nothing — a plain insert that tolerates the duplicate key works against the existing lower(item_name) index.

In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:268-273, replace the upsert with:

    const { error } = await supabase
      .from('shopping_history')
      .insert({ item_name: name, household_id: '92e1a987-99b7-41ec-93fb-ae2ada2bcf72', last_used: new Date().toISOString() });
    // 23505 = the name is already in history, which is the expected no-op
    if (error && error.code !== '23505') console.error('shopping_history write failed', error);

Then await the call at both sites so the row is recorded before the shopping_list row disappears:
- page.tsx:279 in deleteShoppingItem → `await rememberItemName(shopItem.item_name);`
- page.tsx:294 in moveShoppingToInventory → `await rememberItemName(shopItem.item_name);`

If you would rather keep a real upsert (to refresh last_used), the schema needs a column-level target the client can name, e.g. add a 

### 59. [medium] No baseline migration: inventory_items, shopping_list, tasks, category_order are never CREATE'd anywhere in the repo
**תיקון:** Add one idempotent baseline file, /Users/shukishmuely/Documents/Claude/smart-home-inventory/supabase-migration-000-baseline.sql, generated from the live schema (Supabase dashboard - Database - Schema, or `pg_dump --schema-only -t inventory_items -t shopping_list -t tasks -t category_order`) rather than hand-guessed, so the shipped DDL matches production exactly. It must cover at minimum every column page.tsx touches:

CREATE TABLE IF NOT EXISTS inventory_items — id uuid PK default gen_random_uuid(), item_name text not null, quantity int default 0, category text default 'כללי', location text default 'מזווה', updated_at timestamptz default now(), household_id uuid default '92e1a987-99b7-41ec-93fb-ae2ada2bcf72'
CREATE TABLE IF NOT EXISTS shopping_list — id, item_name text not null, category text default 'כללי', category_auto boolean default false, household_id uuid with the same default
CREATE TABLE IF NOT EXISTS tasks — id, title text not null, description text, urgency text, assignee text, target_date date, status text, created_at timestamptz default now(), depends_on_task_id uuid references tasks(id) on delete set null
CREATE TABLE IF NOT EXISTS category_order — id, category_name t

### 60. [high] Category rename/delete skip inventory_items, so the old category reappears as an un-deletable phantom
**תיקון:** Add the missing inventory_items migration to both handlers in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx (two lines), and check the errors before mutating local state:

saveRenameCategory (after line 571):
    await supabase.from('inventory_items').update({ category: newCat }).eq('category', oldCat);

deleteCategory (after line 580):
    await supabase.from('inventory_items').update({ category: 'כללי' }).eq('category', cat);

Skip the `.is('category', null)` handling — 'כללי' is not renamable or deletable from the UI (guard at page.tsx:1683), so NULL rows are never the source or target of these operations. Optionally capture the `error` from each call and bail out of the local `setCategories` update plus show a failure status, so a partial failure does not leave the UI claiming success.

### 61. [medium] Template seeds are unguarded INSERTs and are shipped twice, so re-running a migration file duplicates every template
**תיקון:** Minimal, no app change: guard each of the three seeds in both files so they are idempotent, e.g. in supabase-migration-shopping-templates.sql:26-30 and the identical block at supabase-הרצה-אחת-מסודרת.sql:41-45, replace the bare VALUES insert with

    WITH t AS (
      INSERT INTO shopping_templates (name, household_id)
      SELECT 'סושי', '92e1a987-99b7-41ec-93fb-ae2ada2bcf72'
      WHERE NOT EXISTS (
        SELECT 1 FROM shopping_templates
        WHERE name = 'סושי'
          AND household_id = '92e1a987-99b7-41ec-93fb-ae2ada2bcf72'
      )
      RETURNING id
    )

and the same for 'לחמניות ללג' and 'פסטיבלים וקמפינג'. With no rows returned by t, the dependent `FROM t, (VALUES ...)` item insert cross-join produces zero rows on a re-run, so the items need no separate guard. Then change the "להריץ פעם אחת בלבד" header of supabase-הרצה-אחת-מסודרת.sql:8 to say the file is idempotent, and add one line saying it is the same content as the individual migration files.

Only if you also want the DB to enforce it: add CREATE UNIQUE INDEX IF NOT EXISTS shopping_templates_name_household_uniq ON shopping_templates (household_id, name) — but in the same change give createTemplate (src/app/

### 62. [medium] Fire-and-forget task-completion write with the error discarded: fireworks + card move happen even when the UPDATE fails
**תיקון:** Minimal fix that keeps the concurrent animation — capture the error and roll back with a toast instead of discarding it. Replace line 999:

    supabase.from('tasks').update({ status: newStatus }).eq('id', id).then(({ error }) => {
      if (!error) return;
      showStatus('❌ שמירת המשימה נכשלה', true);
      setTasks(prev => prev.map(t => t.id === id ? { ...t, status: t.status } : t));
      setPoofingTaskId(null);
      setCelebration(null);
    });

If a simpler, stricter behavior is acceptable, await the write before starting the animation and bail on error:

    const { error } = await supabase.from('tasks').update({ status: newStatus }).eq('id', id);
    if (error) { showStatus('❌ שמירת המשימה נכשלה', true); return; }
    setPoofingTaskId(id);
    setCelebration(buildCelebration());
    ...

(the rollback variant needs the pre-update status captured before the optimistic flip, e.g. `const prevStatus = tasks.find(t => t.id === id)?.status;`, since the +1500ms setTimeout may already have run).

Separately, for the other writes (236-237, 249-250, 258-259, 278-280, 287-295, 1009-1010, 1014-1015) the low-cost version of the same fix is a tiny helper that checks `{ error }` once a

### 63. [medium] fetchData swallows read errors on 7 of 8 tables (only templates is surfaced), and the client-state-only guard on category_order inserts lets duplicate category rows accumulate
**תיקון:** 1. Surface all read errors, mirroring the templatesError treatment. In fetchData (page.tsx:148-196), collect them into one state instead of adding six banners:

    const failed = [
      ['מלאי', invRes.error], ['קניות', shopRes.error], ['קטגוריות', catsRes.error],
      ['מטלות', tskRes.error], ['ציוד', equipRes.error], ['היסטוריה', histRes.error],
      ['רשימות', tplRes.error || tplItemsRes.error],
    ].filter(([, e]) => e);
    setLoadErrors(failed.map(([label, e]: any) => `${label}: ${e.message}`));

and render one red banner when loadErrors.length > 0, keeping the existing templates banner or folding it into this one.

2. Stop trusting client state for the category_order insert. Minimal, no schema change needed: skip the insert entirely when the categories read failed (if catsRes.error, `categories` is unknown, not empty), and replace the five `.insert(...)` calls with a DB-level guard. Cheapest version that needs no migration:

    const { data: existing } = await supabase.from('category_order').select('id').eq('category_name', cat).limit(1);
    if (!existing?.length) await supabase.from('category_order').insert([{ category_name: cat, sort_order: 99 }]);

Better, if you c

### 64. [low] shopping_list.category_auto is write-only at one of five insert paths — the migration's triage feature was never wired to it
**תיקון:** Take the smaller of the two options the auditor offered: delete the flag rather than finish it, since the feature it was meant to power already works off `category === 'כללי'`.

1. src/app/page.tsx — drop line 711 (`category_auto: !it.category,`), leaving the insert as item_name + category like the other four paths.
2. Delete supabase-migration-shopping-category-auto.sql and remove the `'עמודה shopping_list.category_auto'` check block at supabase-בדיקת-מצב.sql:30-38, plus the same ALTER/UPDATE pair at supabase-הרצה-אחת-מסודרת.sql:196-203, so the state check stops reporting a column nothing uses.
3. Optional, only if the column should really disappear from the DB (the user has already run the migration per תיעוד_עבודה_שוטף.md:42): `ALTER TABLE shopping_list DROP COLUMN IF EXISTS category_auto;` — safe now precisely because no code reads it. Leaving it in place is also fine; it just becomes an unused column.

If instead the flag is wanted later, the correct completion is bigger than one line: set `category_auto` at all five insert sites based on whether the category came from `guessCategories()`/`/api/parse` rather than from the user, clear it to false in applyCategorizeAssignments (

### 65. [low] Template delete (and inventory delete) drop the last copy of a product name without recording it in shopping_history, so it leaves the autocomplete pool permanently
**תיקון:** Await `rememberItemName` before the destructive write in the two delete paths (fix the `onConflict` mismatch first, or these calls are no-ops).

deleteInventoryItem (page.tsx:256):
    const deleteInventoryItem = async (item: Item) => {
      if (!confirm(`למחוק לצמיתות את "${item.item_name}"?`)) return;
      await rememberItemName(item.item_name || '');
      setInventory(prev => prev.filter(i => i.id !== item.id));
      await supabase.from('inventory_items').delete().eq('id', item.id);
    };

Note `rememberItemName` is declared at page.tsx:264, after this function — fine at runtime (both are `const` arrow functions in the same component body, called only from event handlers), but move the declaration above `deleteInventoryItem` if you prefer source order.

deleteTemplate (page.tsx:768):
    const deleteTemplate = async (template: ShoppingTemplate) => {
      if (!confirm(`למחוק את הרשימה "${template.name}" לצמיתות?`)) return;
      for (const it of template.items || []) await rememberItemName(it.item_name || '');
      await supabase.from('shopping_templates').delete().eq('id', template.id);
      ...
    };

Optional, only if you want pre-rename names kept: in saveEditedName,

### 66. [medium] moveShoppingToInventory matches inventory by raw decorated name and omits updated_at, creating duplicate inventory rows with a stale date
**תיקון:** Normalize on both sides and store the cleaned name, plus stamp the date. Replace page.tsx:283-293 with:

  const moveShoppingToInventory = async (shopItem: any, qty: number) => {
    const cleanName = stripName(shopItem.item_name || '') || shopItem.item_name;
    const key = cleanName.toLowerCase();
    const existing = inventory.find(i => stripName(i.item_name || '').toLowerCase() === key);
    if (existing) {
      const newQty = (existing.quantity || 0) + qty;
      await supabase.from('inventory_items')
        .update({ quantity: newQty, updated_at: new Date().toISOString() })
        .eq('id', existing.id);
    } else {
      await supabase.from('inventory_items').insert([{
        item_name: cleanName, quantity: qty, category: shopItem.category || 'כללי',
        location: 'מזווה', household_id: '92e1a987-99b7-41ec-93fb-ae2ada2bcf72'
      }]);
    }
    ...

(stripName is in scope — it is defined in the same component at :1121 and only evaluated when the handler runs.)

Secondary, same file, lower impact: the inventory branch of saveItemWithCategory uses the same raw equality at page.tsx:316 `inventory.find(i => i.item_name === name)`; once decorated names have leaked into 

### 67. [no] Word-choice ("למה התכוונת?") flow is unreachable dead code — handleUpdate only runs in INVENTORY view
**תיקון:** Pick one, do not leave it as is.

If the feature is not wanted (minimal, safest): delete the whole unreachable set in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx — the `WordChoiceTask` type (28), the `wordChoiceTasks` state (94), `stripPrefix` (359) and `normalizeShopName` (358), the entire `if (activeView === 'SHOPPING') { ... }` block inside the add-item arm (397-424), the `wordChoiceTasksLocal` declaration (357) plus the `duplicateShoppingItems` declaration and its now-always-empty branches (441-442, 447-448, 435-437, 451), `handleResolveWordChoice` (484-495), and the JSX panel (1536-1568). Also drop the `activeView === 'SHOPPING' ? ... :` ternaries at 369-370 in favour of the inventory table/list, since that path only ever runs in INVENTORY. Keep `duplicateShoppingAlerts` and its panel at 1595 — `handleQuickAddSubmit` (657) and `addTemplateToShopping` still populate it. Verify with `npx tsc --noEmit` afterwards.

If the feature is wanted: port it into `handleQuickAddSubmit`. Inside its `for (const row of rows)` loop, after the existing duplicate check and before the insert, split `base` on whitespace, test each word (and its Hebrew-prefix-stripped

### 68. [no] Emoji-prefixed names bypass shopping-list duplicate detection (normalize vs stripName)
**תיקון:** Hoist one helper to module scope (above the component) in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:

const stripName = (n: string) => (n || '').replace(/^[^A-Za-z0-9֐-׿]+/, '').replace(/\s*\(\d+\)\s*$/, '').trim();
const normalizeItemName = (n: string) => stripName(n).toLowerCase();

Then delete the six local copies (lines 358, 551, 590, 619, 683, 816) and the component-local `stripName` (1121-1124), replacing every `normalize(...)` / `normalizeShopName(...)` call with `normalizeItemName(...)`.

One caveat the original proposal misses: the guess map must be re-keyed with the same helper or category lookups silently break. `guessCategories` keys its result at line 604 (`guesses[normalize(itemName)] = rawCat`) and callers read it at lines 552, 636, 640, 704 (`guesses[base]`). If only the dedupe sites switch to the emoji-stripping variant while line 604 keeps the old one, `guesses['עגבניות']` will no longer match a key stored as '🍅 עגבניות' (or vice versa) and the item silently falls back to 'כללי'. Convert lines 552, 590/604, 619ff, 683ff, 816ff together — all keys and all lookups — in the same change.

Insert paths can keep storing the raw name (emo

### 69. [no] `Item.updated_at` is missing from the type; `any` props hide the read
**תיקון:** One line, zero behavioral risk — add the field to the type at line 5 of /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:

`type Item = { id?: string; item_name: string; name?: string; quantity: number; category: string; location: string; needs_classification?: boolean; options?: string[]; removeAll?: boolean; updated_at?: string };`

Optional (`?`) is correct: rows created before the column was populated, and objects built elsewhere in the file, legitimately lack it, and the render at 2322 already guards with `item.updated_at &&`.

Do NOT bundle the auditor's second half ("replace `: any` on InventoryCard/ShoppingCard/ClassificationCard with declared props interfaces") into this fix. It is a separate refactor across four components (lines 2248, 2285, 2305, 2361), each taking 4-19 props including callbacks whose signatures are currently unconstrained; it will surface an unknown number of further errors and carries real regression risk on a working app. It is worth doing as its own scoped change, not as the fix for a missing type member. Verify with `npx tsc --noEmit` and expect the pre-existing `/opi/parse` validator error to remain (that one is unrelated 

### 70. [medium] `saveItemWithCategory` routes by `activeView`, so a pending inventory item can be filed into `shopping_list`
**תיקון:** Carry the origin on the pending item and route by it, and update by `id` when the item already exists in the DB.

1. Tag at creation. In `fetchData` (163): `setPendingItems(prev => [...prev, { ...i, needs_classification: true, origin: 'inventory' }])`. In `handleUpdate` (450): `setPendingItems(uncertainItems.map((i: any) => ({ ...i, item_name: i.name || i.item_name || 'פריט', origin: activeView === 'SHOPPING' ? 'shopping' : 'inventory' })))`.

2. Route by it in `saveItemWithCategory`, replacing the `activeView` test at 316/325:
```
const dest: 'inventory' | 'shopping' =
  item.origin ?? (activeView === 'SHOPPING' ? 'shopping' : 'inventory');
if (dest === 'inventory') { ... } else { ... }
```
`activeView` stays only as the fallback for the direct add paths that have no origin.

3. In the inventory branch, prefer the row id so a `fetchData`-sourced pending item is re-categorised instead of duplicated:
```
if (item.id) {
  await supabase.from('inventory_items').update({ category: cat }).eq('id', item.id);
} else { /* existing find-by-name / insert logic */ }
```
Add `origin?: 'inventory' | 'shopping'` to the `Item` type at line 5.

### 71. [medium] Header 🔍 search does nothing on TASKS/EQUIPMENT/TEMPLATES/SETTINGS, and duplicates the dedicated search box on INVENTORY/SHOPPING
**תיקון:** Delete the superseded header search; the dedicated box already covers the only two views that support it. Four edits in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:

1. Line 82 — remove `const [isSearchOpen, setIsSearchOpen] = useState(false);`
2. Line 1219 — remove the `{activeView !== 'HOME' && <button ... 🔍</button>}` element from the header button row.
3. Lines 1237-1241 — remove the whole `{isSearchOpen && activeView !== 'HOME' && ( ... )}` block.
4. Line 229 — drop `setIsSearchOpen(false);` from `changeView`, keeping `setSearchTerm('')` so switching views still clears the dedicated box.

Nothing else reads `isSearchOpen`, so this is behavior-preserving for INVENTORY/SHOPPING and removes the dead affordance elsewhere.

If keeping a header entry point is wanted instead, the smaller change is to gate line 1219 on `(activeView === 'INVENTORY' || activeView === 'SHOPPING')` and have the header input scroll/focus the existing box rather than render a second one — but that still leaves two inputs unless the header block is dropped, so option 1 is preferable.

Out of scope but worth noting while in this code: `.includes(searchTerm)` is case- and whitesp

### 72. [medium] Category rename/delete updates shopping_list only, leaving inventory_items with the stale name (which resurrects the category)
**תיקון:** Mirror the two writes onto `inventory_items` in src/app/page.tsx.

In `saveRenameCategory` (after line 571):

  await supabase.from('inventory_items').update({ category: newCat }).eq('category', oldCat);

In `deleteCategory` (after line 580):

  await supabase.from('inventory_items').update({ category: 'כללי' }).eq('category', cat);

Both run before the existing `fetchData()`, so the refreshed `displayCategories` no longer re-derives the stale name. No other change needed.

### 73. [low] Same `(N)`-stripping normalization duplicated 8 times in page.tsx
**תיקון:** Add one module-scope helper above `export default function HomePage()` (line 74) in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:

const normalizeName = (n: string) => (n || '').replace(/\s*\(\d+\)\s*$/, '').trim().toLowerCase();

Then:
1. Delete the five local `const normalize = ...` declarations at lines 551, 590, 619, 683, 816 and rename their call sites to `normalizeName` (or keep the name by aliasing once: `const normalize = normalizeName;` — cleaner to just rename).
2. Delete `const normalizeShopName` at line 358; rename its uses at 399-402 and 411-412 to `normalizeName`.
3. Replace the two inline copies in `handleResolveWordChoice`:
   line 486 → `const baseName = normalizeName(chosen);`
   line 487 → `const existsInList = shoppingList.some(s => normalizeName(s.item_name || '') === baseName);`
4. Keep `stripName` (line 1121) as its own function but build it on the shared rule so the two cannot drift further, e.g. strip the leading emoji run first and then reuse the same trailing-`(N)` regex from one shared constant.

Mechanical and behavior-preserving; verify with `npx tsc --noEmit` (or the project's build) plus a grep showing zero remaining `\(

### 74. [low] Shared `input` (and `status`) state leaks between the INVENTORY and TEMPLATES/equipment textareas on view change
**תיקון:** Minimal one-line change in `changeView` (page.tsx:229) — clear both leaked states along with the ones already reset:

    const changeView = (view: typeof activeView) => {
      setActiveView(view); setIsMenuOpen(false); setIsSearchOpen(false); setSearchTerm(''); setAddedSummary([]); setInput(''); setStatus('');
    };

If clearing `status` is unwanted (a message the user should still see after navigating), clear at least `input`; the safer structural alternative is a second state, e.g. `equipInput`, used by the TEMPLATES textarea and `handleEquipmentUpdate`.

### 75. [low] UTC `toISOString()` date used as "today" — off by a day between midnight and 03:00 Israel time (the timer-cleanup half of the claim is not a defect)
**תיקון:** Add one local-date helper at module scope in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx and use it at all five sites (75, 1029, 1041, 1054, 2286):

const localToday = () => new Date().toLocaleDateString('sv-SE'); // YYYY-MM-DD, local tz

Then `const today = localToday();` (75, 2286) and `const todayStr = localToday();` (1029, 1041, 1054). Note the equipment fix needs all three sites changed together — changing only the comparison at 1041 while the writers at 1030/1055 still stamp UTC would make the mismatch permanent for those 3 hours. Rows already stored with a UTC date self-heal after one reset cycle; no migration needed. Skip the statusTimeoutRef cleanup effect, or add it only as unrelated tidy-up.

### 76. [high] A 500 from /api/parse is treated as success: the typed text is cleared and the "מעבד..." pill hangs forever
**תיקון:** Minimal change, both handlers. In `handleUpdate` (page.tsx ~338) and `handleEquipmentUpdate` (~1064), guard before touching state and only clear the input on confirmed success:

      const res = await fetch('/api/parse', { ... });
      const data = await res.json().catch(() => null);
      if (!res.ok || !data || data.error) {
        showStatus('❌ שגיאה בעיבוד', true);
        return;
      }

Then move `setInput('')` inside the success path (or keep it after the guard, which the early `return` now protects), and add an else-branch for the empty case so the pill is never left on 'מעבד...':

      if (data.items && data.items.length > 0) {
        ... existing block ...
      } else {
        showStatus('🤔 לא זיהיתי פריטים', true);
      }
      setInput('');

Optionally wrap the whole body in `try { ... } finally { }` and assert in the finally that `status !== 'מעבד...'`, but the explicit else-branch plus the `res.ok` guard already covers every exit. Note the fix must be applied twice — the two handlers are independent copies.

### 77. [high] Shopping items are deletable only by touch swipe (no desktop/keyboard path), and the swipe bypasses the confirm
**תיקון:** Two small edits in ShoppingCard, both in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx.

1) Add a real delete control next to "📦 הוסף למלאי" (after line 2424). Calling `onDelete(item)` with no second argument keeps the `skipConfirm = false` default at line 276, so this path goes through `confirm()` like every other delete:

  <button type="button" onClick={() => onDelete(item)} aria-label={`מחק ${item.item_name || 'פריט'}`} title="מחק לצמיתות" className="w-8 h-8 rounded-full bg-slate-100 text-slate-400 hover:bg-red-100 hover:text-red-600 font-black text-xs pointer-events-auto shadow-sm transition-colors active:scale-90">🗑️</button>

This alone closes the desktop/keyboard gap and restores a confirmed path. Keep the swipe as a touch accelerator.

2) Make the swipe harder to trigger by accident, at line 2386:

  axisLocked.current = (Math.abs(dx) > Math.abs(dy) * 1.5) ? 'x' : 'y';

Optionally also drop the `true` at line 2397 so the swipe confirms too — but with (1) in place the swipe can reasonably stay confirm-free, since it is now the accelerator rather than the only route. If it stays confirm-free, the honest minimum is an undo: hold the delete behind 

### 78. [medium] No loading state on the first fetch, and a failed read renders as an empty, confident house
**תיקון:** Minimal, three edits, all inside page.tsx:

1) Track load outcome in fetchData:

    const [loadState, setLoadState] = useState<'loading'|'ok'|'error'>('loading');
    const [loadError, setLoadError] = useState<string | null>(null);

In fetchData, wrap the Promise.all in try/catch, and after it collect the errors:

    const firstErr = [invRes, shopRes, catsRes, tskRes, equipRes, histRes]
      .map(r => (r as any).error).find(Boolean);
    setLoadError(firstErr ? (firstErr.message || 'שגיאה בטעינת הנתונים') : null);
    setLoadState(firstErr ? 'error' : 'ok');

(catch → setLoadState('error') with the thrown message.)

2) One banner above the view switch, plus hold the HOME counters until the first success — render `—` instead of a count while `loadState === 'loading'`, and keep the last good numbers on 'error' with the banner explaining they are stale:

    {loadState === 'error' && (
      <div className="bg-red-50 border-2 border-red-200 p-4 rounded-2xl mb-4">
        <p className="font-black text-red-800 text-sm">⚠️ הנתונים לא נטענו</p>
        <p className="text-xs font-bold text-red-600">מה שמוצג אינו מעודכן. נסה לרענן.</p>
        <p className="text-[11px] font-mono text-red

### 79. [high] Every mutation is optimistic and unchecked — offline the UI confirms writes that never happened
**תיקון:** Minimal, no refactor — inspect `error` and roll back at the eight sites, using the file's existing `showStatus('❌ ...', true)` idiom.

1. Add one helper next to showStatus (L142):
   const failed = (res: { error: unknown | null }) => {
     if (!res.error) return false;
     showStatus('❌ לא נשמר — נסה שוב', true);
     fetchData();   // re-sync from the server, which discards the optimistic state
     return true;
   };
   `fetchData()` is the cheapest correct rollback here — it already refetches every table this file mutates, so no per-site inverse patch is needed.

2. Change each `await supabase...` mutation to keep its result and pass it through, e.g.
   L237: const res = await supabase.from('inventory_items').update({ quantity: finalQty, updated_at: now }).eq('id', item.id);
         if (failed(res)) return;
   Same one-line treatment at L250 (updateInventoryField), L259, L280, L1013 (the non-completion branch of updateTaskStatus), L1022, L1053+L1055, L1098.

3. addToShopping (L861-865): move the ✅ behind the check —
   const res = await supabase.from('shopping_list').insert([...]);
   if (failed(res)) return;
   setLowStockAlerts(...); showStatus('✅ נוסף לקניות', true); fetch

### 80. [medium] Task save fails silently on insert error, and WhatsApp hand-off replaces the whole SPA via window.location.href
**תיקון:** Minimal change, all inside src/app/page.tsx:

Line 887 — guard the date before splitting:

    const dateParts = (t.target_date || '').split('-');
    const formattedDate = dateParts.length === 3 ? `${dateParts[2]}/${dateParts[1]}/${dateParts[0]}` : '';

and only append the date line when `formattedDate` is non-empty.

Lines 898-900 — report both bail-outs instead of returning mutely:

    if (!newTask.title.trim()) { showStatus('❌ חסר שם למשימה', true); return; }
    const { error } = await supabase.from('tasks').insert([newTask]);
    if (error) { showStatus('❌ המשימה לא נשמרה', true); return; }

and add `showStatus('✅ המשימה נשמרה', true)` after the successful reset so the ordinary 💾 שמור path confirms itself.

Line 906 — keep the app mounted:

    window.open(buildWhatsAppLink(taskCopy), '_blank', 'noopener');

No change is needed in globals.css; the second file in the report is not involved.

### 81. [medium] Operation feedback lives in a plain span (never announced) and is rendered twice at once in the קניות view
**תיקון:** Two small edits, no refactor of the state shape needed:

1. Delete the second pill. Remove lines 2081-2085 (the `{status && (...)}` block inside the `showTemplatesPanel` panel) entirely — the quick-add pill at 1456-1460 is always mounted in the SHOPPING view, so template feedback still shows, just once.

2. Make the remaining pills announce. On each of the three surviving wrappers (1362, 1457, 1949) add the live-region attributes to the existing div:

`<div role="status" aria-live="polite" aria-atomic="true" className="mt-4 flex justify-center animate-in fade-in slide-in-from-bottom-2">`

Because only one view renders at a time, at most one live region is mounted, so there is no double announcement. Note the wrapper is currently conditional on `{status && ...}`, which means the region is created at the same moment the text appears — some screen readers skip that. To be safe, drop the `{status && (` guard and let the div render always with the span inside guarded instead, so the region exists before the text lands:

```
<div role="status" aria-live="polite" aria-atomic="true" className="mt-4 flex justify-center">
  {status && <span className={...}>{status}</span>}
</div>
```

Option

### 82. [medium] Rename spans and the HOME title are clickable non-focusable elements — no keyboard route to rename an item or return home
**תיקון:** Minimal change, four elements, look preserved (line 2445 already demonstrates the pattern with `bg-transparent border-none p-0`):

1. page.tsx:2320 — swap the span for a button, keeping the same handler and classes:
   <button type="button" onClick={() => {setEditingId(item.id); setEditNameValue(item.item_name || 'פריט לא ידוע');}} className="block w-full text-right bg-transparent border-none p-0 font-bold text-lg text-slate-800 cursor-pointer hover:text-[var(--c-primary)] hover:underline decoration-dashed decoration-slate-300 pointer-events-auto transition-colors">{item.item_name || 'פריט לא ידוע'}</button>

2. page.tsx:2432 — identical swap (span -> button type="button", add `text-right bg-transparent border-none p-0`).

3. page.tsx:1973 — identical swap (add `text-right bg-transparent border-none p-0`, keep `flex-1`).

4. page.tsx:1217 — wrap the title text in a button so HOME is reachable:
   <h1 className="text-2xl font-black drop-shadow-md"><button type="button" onClick={() => changeView('HOME')} className="bg-transparent border-none p-0 font-black text-inherit cursor-pointer">הבית של ניאו 🏠</button></h1>
   (Or, cheaper: add a `<button ... onClick={() => changeView('HOME')}>

### 83. [no] Pack/unpack toggle button has no accessible name or checked state
**תיקון:** Two one-line edits in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx — turn both buttons into real checkboxes with a name. No globals.css change is needed (no sr-only class required).

Line 1966, add role/aria-checked/aria-label:

    <button onClick={() => togglePacked(item)} role="checkbox" aria-checked={false} aria-label={`${item.item_name} — סמן כנארז`} className="w-7 h-7 rounded-lg border-2 border-sky-300 bg-white flex items-center justify-center hover:bg-sky-50 transition-colors pointer-events-auto flex-shrink-0" />

Line 1999, same treatment, and hide the decorative glyph from AT:

    <button onClick={() => togglePacked(item)} role="checkbox" aria-checked={true} aria-label={`${item.item_name} — נארז, בטל סימון`} className="w-7 h-7 rounded-lg border-2 border-emerald-400 bg-[var(--c-primary)] flex items-center justify-center pointer-events-auto">
      <span aria-hidden="true" className="text-white text-sm font-black">✓</span>
    </button>

(If preferred, use `aria-checked={!!item.is_packed}` in both places instead of the literals — the two branches are already split by packed state, so the literals are correct either way.) The report's extra sugg

### 84. [medium] Quick-add autocomplete is pointer-only: no onKeyDown, no ARIA, and onMouseDown makes keyboard activation of an option a no-op
**תיקון:** Minimal, contained to the 1385-1413 block plus one state hook.

1. Add highlight state next to line 115:
```tsx
const [suggestIdx, setSuggestIdx] = useState(-1);
```

2. Reset it whenever the query changes — inside the existing onChange (1388) and onFocus (1389): `setSuggestIdx(-1)`.

3. Add an onKeyDown to the input (guard so Enter only intercepts when an option is highlighted, preserving today's submit-on-Enter):
```tsx
onKeyDown={(e) => {
  if (!matches.length) return;
  if (e.key === 'ArrowDown') { e.preventDefault(); setSuggestIdx(p => (p + 1) % matches.length); }
  else if (e.key === 'ArrowUp') { e.preventDefault(); setSuggestIdx(p => (p <= 0 ? matches.length : p) - 1); }
  else if (e.key === 'Escape') { e.preventDefault(); setSuggestRow(null); setSuggestIdx(-1); }
  else if (e.key === 'Enter' && suggestIdx >= 0) {
    e.preventDefault();
    const m = matches[suggestIdx];
    updateQuickAddRow(i, { name: m }); setSuggestRow(null); setSuggestIdx(-1); autoFillRowCategory(i, m);
  }
}}
```

4. Switch the option handler from `onMouseDown` to `onClick`, keeping `onMouseDown` only to suppress the blur so focus stays in the input (which also sidesteps the 150 ms race — no blur, no 

### 85. [low] Menu dropdown lacks aria-expanded/haspopup and Escape; search close drops focus to body
**תיקון:** Three small edits in /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, no new dependencies:

1. Add a ref near line 140:
   const menuBtnRef = useRef&lt;HTMLButtonElement&gt;(null);
   const searchBtnRef = useRef&lt;HTMLButtonElement&gt;(null);

2. Line 1222 — annotate the trigger and let Escape close it:
   &lt;button ref={menuBtnRef} aria-expanded={isMenuOpen} aria-haspopup="menu" onClick={() =&gt; setIsMenuOpen(!isMenuOpen)} onKeyDown={(e) =&gt; { if (e.key === 'Escape' &amp;&amp; isMenuOpen) setIsMenuOpen(false); }} ...&gt;
   and on the panel div (1226) add role="menu" plus
   onKeyDown={(e) =&gt; { if (e.key === 'Escape') { setIsMenuOpen(false); menuBtnRef.current?.focus(); } }}
   so Escape works whether focus is on the trigger or inside the panel.

3. Line 1219 — keep the ref and return focus when the search closes:
   &lt;button ref={searchBtnRef} aria-expanded={isSearchOpen} onClick={() =&gt; { const next = !isSearchOpen; setIsSearchOpen(next); if (!next) searchBtnRef.current?.focus(); }} ...&gt;
   and on the input (1239) add
   onKeyDown={(e) =&gt; { if (e.key === 'Escape') { setIsSearchOpen(false); searchBtnRef.current?.focus(); } }}

Skip the

### 86. [low] Form controls have no accessible names; the three <label>s are unassociated and the quantity select is an invisible focus target
**תיקון:** Two mechanical passes, no restructuring.

1. Give the unnamed controls an `aria-label` (Hebrew, matching the adjacent visible text):
- page.tsx:1750 → `aria-label="דחיפות"`; 1753 → `aria-label="אחראי"`; 1757 → `aria-label="תאריך יעד"`.
- Same three in the inline task editor: 1832 → `aria-label="דחיפות"`; 1841 → `aria-label="אחראי"`; 1850 → `aria-label="תאריך יעד"`.
- Quick-add category select (1414) → `aria-label="קטגוריה"`; quantity select (2332) → `aria-label={`כמות של ${item.item_name}`}`.
- Placeholder-only text fields keep the placeholder as an example and add `aria-label` with the same words: 1239 (search), 1327 (search), 1386 (`aria-label="שם פריט"`), 1429 (`aria-label="קטגוריה חדשה"`), 1747/1748 (`aria-label="שם המשימה"` / `aria-label="תיאור"`), 2049/2056 (`aria-label="שם הרשימה"` / `aria-label="פריטי הרשימה"`).

2. Pair the three real labels — add `id` to the control and `htmlFor` to the label so clicking the text focuses the field:
- 1759/1760 → `htmlFor="new-task-depends"` + `id="new-task-depends"`.
- 1857/1859 → `htmlFor={`edit-depends-${task.id}`}` + matching `id`.
- 2295/2297 → `htmlFor={`restore-date-${task.id}`}` + matching `id`.

3. Make the quantity focus perceiva

### 87. [low] Segmented toggles carry no aria-pressed / aria-current (selected state is not exposed to assistive tech)
**תיקון:** One attribute per toggle, no restructuring:

- page.tsx:1318 add `aria-pressed={sortBy === 'name'}`; 1319 add `aria-pressed={sortBy === 'category'}`
- page.tsx:1629 add `aria-pressed={invFilter === f}`
- page.tsx:1925 add `aria-pressed={equipListType === lt}`
- task status buttons: `aria-pressed={task.status === 'לא התחלתי' || !task.status}` (1878-1884), `aria-pressed={task.status === 'בתהליך'}` (1885-1891), `aria-pressed={false}` on the 'סיימתי' button (1892-1898) — it is only ever an action there, since a finished task moves to the completed section.

Optionally strengthen only the weak sort pair at 1318-1319, e.g. add `ring-1 ring-indigo-300` (or `underline`) to the active branch. Do not "fix" line 1892's missing active branch and do not add a low-stock text badge on the strength of this report — the first is unreachable code and the second is not colour-only.

### 88. [low] deleteTask's confirm() names no task, so a mis-tap hard-deletes an unidentified task with no recovery
**תיקון:** Pass the task (or its title) into `deleteTask` and name it in the prompt, matching the other five call sites. In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx:

Change the signature at 1018 to `const deleteTask = async (id: string, title?: string) => {` and line 1019 to:
`if (confirm(`למחוק את המשימה "${title || tasks.find(t => t.id === id)?.title || ''}" לתמיד?`)) {`

Then update the two callers: 1797 `deleteTask(task.id!, task.title)` and 1909 `onDelete={(id: string, title?: string) => deleteTask(id, title)}` (or simply `onDelete={deleteTask}` and change 2292 to `onDelete(task.id!, task.title)`).

Nothing else in the finding needs changing — do not rip out the other five `confirm()` calls or the swipe path.

### 89. [low] Task-completion celebration (~280 animated nodes) has no prefers-reduced-motion escape hatch and no aria-hidden
**תיקון:** Two edits, no new dependencies.

1) Append to /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/globals.css (after line 185):

    @media (prefers-reduced-motion: reduce) {
      .animate-firework-particle,
      .animate-firework-flash,
      .animate-balloon-rise,
      .animate-task-poof,
      .float-soft,
      .header-flow,
      .view-anim {
        animation: none !important;
      }
    }

Safe end-states: `.view-anim` uses `both` on a fade-in-to-opacity-1 keyframe, so killing it leaves the view visible; `.animate-task-poof` killed leaves the card fully opaque until the 1500ms state update removes it, which is the desired reduced-motion behaviour.

2) In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, skip building the DOM at all and hide it from assistive tech.

At line 997, replace `setCelebration(buildCelebration());` with:

    const reduceMotion = typeof window !== 'undefined'
      && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (!reduceMotion) setCelebration(buildCelebration());

At line 1167, add `aria-hidden="true"` to the container:

    <div aria-hidden="true" className="fixed inset-0 z-[2000] 

### 90. [medium] Category rename/delete skips inventory_items (and shopping_template_items), so the old category reappears as a ghost heading in מלאי
**תיקון:** In /Users/shukishmuely/Documents/Claude/smart-home-inventory/src/app/page.tsx, extend both writers to every table that stores a category, and gate the local state update on success.

saveRenameCategory (replace lines 571-575):
```ts
const results = await Promise.all([
  supabase.from('shopping_list').update({ category: newCat }).eq('category', oldCat),
  supabase.from('inventory_items').update({ category: newCat }).eq('category', oldCat),
  supabase.from('shopping_template_items').update({ category: newCat }).eq('category', oldCat),
  supabase.from('category_order').update({ category_name: newCat }).eq('category_name', oldCat),
]);
if (results.some(r => r.error)) { showStatus('❌ שינוי שם הקטגוריה נכשל', true); fetchData(); return; }
setCategories(prev => Array.from(new Set(prev.map(c => c === oldCat ? newCat : c))));
```

deleteCategory (replace lines 580-583) — same shape, reassigning to 'כללי' so the confirm text becomes true:
```ts
const results = await Promise.all([
  supabase.from('shopping_list').update({ category: 'כללי' }).eq('category', cat),
  supabase.from('inventory_items').update({ category: 'כללי' }).eq('category', cat),
  supabase.from('shopping_template_items').upda

### 91. [low] One `input` state backs both the מלאי and רשימות ציוד textareas, so unsubmitted text leaks across the view switch
**תיקון:** One line in `changeView` (page.tsx:228-230) — clear the shared free-text draft on every view transition, which is safe because all navigation funnels through this function:

  const changeView = (view: typeof activeView) => {
    setActiveView(view); setIsMenuOpen(false); setIsSearchOpen(false); setSearchTerm(''); setAddedSummary([]); setInput('');
  };

If preserving each view's draft matters more than isolation, the alternative is two states (`invInput` / `equipInput`) wired to lines 1356/1932 and to the reads at 335/338/462 and 1060/1063/1092 respectively — more churn, same defect closed. The one-liner is the minimal fix.

### 92. [low] Chip click on an already-open task editor updates the highlight ring but never moves keyboard focus (autoFocus only fires on mount)
**תיקון:** Drive focus from an effect instead of `autoFocus`. In the component body add one ref map plus an effect:

```
const editFieldRefs = {
  title: useRef<HTMLInputElement>(null),
  urgency: useRef<HTMLSelectElement>(null),
  assignee: useRef<HTMLSelectElement>(null),
  date: useRef<HTMLInputElement>(null),
  depends: useRef<HTMLSelectElement>(null),
};
useEffect(() => {
  if (!editingTaskId) return;
  const key = editTaskFocus === 'all' ? 'title' : editTaskFocus;
  editFieldRefs[key]?.current?.focus({ preventScroll: true });
}, [editingTaskId, editTaskFocus]);
```

Then on lines 1819, 1833, 1841, 1851, 1859 replace each `autoFocus={...}` with the matching `ref={editFieldRefs.title}` / `.urgency` / `.assignee` / `.date` / `.depends`. `preventScroll: true` also removes the viewport jump `autoFocus` causes on a long task list. Note the effect must live at component level, not inside the `tasks.map` callback — the panel is rendered per task but only one task edits at a time, so a single ref set is enough.


## נזרקו באימות

- Req 25 — the chosen font never reaches headings; h1/h2/h3 stay Heebo in 4 of the 5 themes
- Req 24 — theme tokens are scoped to <main>, so the page/selection chrome stays on the pastel palette
- Dead `exactMatch` branch — the documented 'single exact match applies directly' behaviour never happens
- handlePlus/handleMinus compute the new quantity from a captured item, so fast taps are lost
- statusTimeoutRef's 4s timer is never cleared on unmount
- Swipe-delete's 160ms removal timeout is uncancellable
- Equipment daily-reset effect: missing deps and a cancellation that silently skips the reset
- `today` recomputed during render seeds date inputs, risking hydration mismatch and a stale default
- Swipe-to-delete uses raw physical clientX: wrong direction for RTL and collides with the iOS back-swipe
- Date inputs inherit dir=rtl, so the native field's segments and separators lay out mirrored
- Scrollbar gutter padding is on the side that has no scrollbar in RTL
- Mixed Latin/numeric runs and interpolated item names are not bidi-isolated inside Hebrew sentences
- Physical direction utilities used throughout instead of logical ones — correct only because the app is RTL-only
- Swipe only recognises rightward drags, which is the wrong direction for an RTL Hebrew list
- Drag position is stored in React state, so the swipe repaints at commit rate instead of on the compositor
- Autocomplete dropdown renders under the iOS keyboard and is selected via a synthesized mouse event
- Sticky header hides the field that iOS scrolls into view when an inline editor auto-focuses
- Category-assignment panel scrolls internally with no overscroll containment and traps a field under the keyboard
- Unbounded lists re-sorted with per-comparison localeCompare on every render
- No safe-area handling and 80px of reserved bottom padding for a bar that does not exist
- shopping_list and category_order inserts omit household_id while every sibling table's inserts set it
- equipment_sessions read with .single() and no household filter — daily auto-reset never fires for lists with no session row
- The duplicate-template cleanup deletes by name across all households and the camping copy ignores household and category
- No query filters by household_id even though six tables carry the column
- 'EQUIPMENT' is a dead member of the activeView union
- `household_id` UUID hardcoded 7 times, and omitted from every shopping_list insert
- `EditTaskFocus` values `'title'` and `'depends'` are never passed by any caller
- `animate-bounce-short` class is used but defined nowhere
- Hebrew prefix-strip regex duplicated three times
- `handleAddTask` and the task form's `onSubmit` duplicate the WhatsApp button
- `deleteShoppingItem`'s confirm path is dead — no caller omits `skipConfirm`
- `category_auto` is written by one of five shopping_list inserts and never read
- Equipment category resolution contradicts itself and never persists new categories
- `.float-soft` / `@keyframes float-soft` and `@keyframes balloon-sway` are unused
- `@theme inline` block and `--background`/`--foreground` tokens are unused
- Hover-lift rule is inconsistent and silently defeated on shopping cards
- `Item.options` and `Item.removeAll` are dead fields; `table: any` is a gratuitous escape hatch
- Removing outline on 17 controls with no focus-visible replacement leaves parts of the app with no visible focus
- Classification cards duplicate on every refetch because setPendingItems appends inside a filter predicate
